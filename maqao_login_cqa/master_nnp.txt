Target processor is: Intel(R) Xeon(R) Icelake SP (x86_64/Icelake-SP micro-architecture).

[MAQAO] Info: No innermost loops in the function input_cp2k_nnp_mp_create_nnp_section_
[MAQAO] Info: No innermost loops in the function input_cp2k_nnp_mp_create_nnp_bias_print_section_
[MAQAO] Info: No innermost loops in the function input_cp2k_nnp_mp_create_nnp_print_section_
[MAQAO] Info: No innermost loops in the function nnp_environment._
[MAQAO] Info: Assuming lines 556 and 555 correspond to the same source loop
[MAQAO] Info: Assuming lines 554 and 553 correspond to the same source loop
[MAQAO] Info: Assuming lines 533 and 532 correspond to the same source loop
[MAQAO] Info: Assuming lines 386 and 385 correspond to the same source loop
[MAQAO] Info: Assuming lines 535 and 536 correspond to the same source loop
[MAQAO] Info: Assuming lines 534 and 533 correspond to the same source loop
[MAQAO] Info: Assuming lines 558 and 557 correspond to the same source loop
[MAQAO] Info: Assuming lines 559 and 558 correspond to the same source loop
[MAQAO] Info: Assuming lines 560 and 559 correspond to the same source loop
Section 1: Function: nnp_init_model
===================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_environment.F

Section 1.1: Source loop ending at line 386
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2944 (386-386)
 - 2945 (385-385)
and is unrolled by 10 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 2945
 - peel or tail: 2944
The analysis will be displayed for the unrolled and/or vectorized loops: 2945

Section 1.1.1: Binary (unrolled and/or vectorized) loop #2945
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:385.

It is main loop of related source loop which is unrolled by 10 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by:
 - reading data from caches/RAM (load units are a bottleneck)
 - writing data to caches/RAM (the store unit is a bottleneck)

Workaround(s):
 - Read less array elements
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 1.2: Source loop ending at line 393
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 2943
and is not unrolled or unrolled with no peel/tail loop.

Section 1.2.1: Binary loop #2943
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:393.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 4.00 to 3.00 cycles (1.33x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is vectorized, but using 45% register length (average across all SSE/AVX instructions).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 3.40 cycles (1.18x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 1.3: Source loop ending at line 395
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 2941
and is not unrolled or unrolled with no peel/tail loop.

Section 1.3.1: Binary loop #2941
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:395.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by:
 - reading data from caches/RAM (load units are a bottleneck)
 - writing data to caches/RAM (the store unit is a bottleneck)

Workaround(s):
 - Read less array elements
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 1.4: Source loop ending at line 400
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 2939
and is not unrolled or unrolled with no peel/tail loop.

Section 1.4.1: Binary loop #2939
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:400.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 4.40 to 1.00 cycles (4.40x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.40 to 0.55 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.5: Source loop ending at line 409
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2937 (409-409)
 - 2938 (409-409)
and is multi-versionned.
The analysis will be displayed for all loops

Section 1.5.1: Binary loop #2937
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:409.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.50 to 0.31 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 2.50 to 2.20 cycles (1.14x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop



Section 1.5.2: Binary loop #2938
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:409.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.50 to 0.12 cycles (20.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 2.50 to 2.20 cycles (1.14x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 1.6: Source loop ending at line 414
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 2931
and is not unrolled or unrolled with no peel/tail loop.

Section 1.6.1: Binary loop #2931
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:405-408,414.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 4 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 1.6.1.1: Path #1
========================

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 9.40 to 0.79 cycles (11.91x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.6.1.2: Path #2
========================

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 16.00 to 1.28 cycles (12.49x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.6.1.3: Path #3
========================

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 17.60 to 1.35 cycles (13.02x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.6.1.4: Path #4
========================

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 17.00 to 1.29 cycles (13.19x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.6.1.5: Average path
=============================

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 15.00 to 1.18 cycles (12.74x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 1.7: Source loop ending at line 434
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 2936
and is not unrolled or unrolled with no peel/tail loop.

Section 1.7.1: Binary loop #2936
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:430-434.

The related source loop is not unrolled or unrolled with no peel/tail loop.
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 1.7.1.1: Path #1
========================

Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 5.80 to 0.45 cycles (12.80x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.7.1.2: Path #2
========================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.60 to 0.37 cycles (12.57x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.7.1.3: Average path
=============================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 5.20 to 0.41 cycles (12.70x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 1.8: Source loop ending at line 446
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2933 (446-446)
 - 2934 (446-446)
 - 2935 (446-446)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 2935
 - unrolled and/or vectorized peel or tail: 2933
 - peel or tail: 2934
The analysis will be displayed for the unrolled and/or vectorized loops: 2935

Section 1.8.1: Binary (unrolled and/or vectorized) loop #2935
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:446.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 1.9: Source loop ending at line 485
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 2888
and is not unrolled or unrolled with no peel/tail loop.

Section 1.9.1: Binary loop #2888
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:462,474,482-485.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Failed to get the number of paths
 - RecMII not computed since number of paths is unknown or > max_paths

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 7% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 17.80 to 1.22 cycles (14.61x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.10: Source loop ending at line 490
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2930
and is not unrolled or unrolled with no peel/tail loop.

Section 1.10.1: Binary loop #2930
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:488-490.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 6.00 to 0.37 cycles (16.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 6.00 to 5.20 cycles (1.15x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 1.11: Source loop ending at line 511
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2929
and is not unrolled or unrolled with no peel/tail loop.

Section 1.11.1: Binary loop #2929
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:501-511.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=12
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 12 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 23.20 to 1.42 cycles (16.32x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.12: Source loop ending at line 534
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2925 (534-534)
 - 2926 (533-533)
 - 2927 (532-532)
and is multi-versionned.
The analysis will be displayed for all loops

Section 1.12.1: Binary loop #2925
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:534.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.00 to 1.80 cycles (4.44x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 1.00 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 1.12.2: Binary loop #2926
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:533.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.00 to 1.80 cycles (4.44x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 1.00 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 1.12.3: Binary loop #2927
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:532.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.00 to 1.80 cycles (4.44x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 1.00 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.13: Source loop ending at line 535
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2923 (536-536)
 - 2924 (535-535)
and is unrolled by 2 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 2923
 - peel or tail: 2924
The analysis will be displayed for the unrolled and/or vectorized loops: 2923

Section 1.13.1: Binary (unrolled and/or vectorized) loop #2923
==============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:536.

It is main loop of related source loop which is unrolled by 2 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 0.52 cycles (15.52x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.14: Source loop ending at line 552
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2922
and is not unrolled or unrolled with no peel/tail loop.

Section 1.14.1: Binary loop #2922
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:552.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.00 to 1.80 cycles (4.44x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 1.00 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.15: Source loop ending at line 554
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2920 (554-554)
 - 2921 (553-553)
and is multi-versionned.
The analysis will be displayed for all loops

Section 1.15.1: Binary loop #2920
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:554.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.00 to 1.80 cycles (4.44x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 1.00 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 1.15.2: Binary loop #2921
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:553.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.00 to 1.80 cycles (4.44x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 1.00 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.16: Source loop ending at line 556
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2918 (556-556)
 - 2919 (555-555)
and is multi-versionned.
The analysis will be displayed for all loops

Section 1.16.1: Binary loop #2918
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:556.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.00 to 1.80 cycles (4.44x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 1.00 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 1.16.2: Binary loop #2919
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:555.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.00 to 1.80 cycles (4.44x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 1.00 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.17: Source loop ending at line 560
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2914 (560-560)
 - 2915 (559-559)
 - 2916 (558-558)
 - 2917 (557-557)
and is unrolled by 2 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 2914, 2915
 - peel or tail: 2916, 2917
The analysis will be displayed for the unrolled and/or vectorized loops: 2914, 2915

Section 1.17.1: Binary (unrolled and/or vectorized) loop #2914
==============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:560.

It is main loop of related source loop which is unrolled by 2 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 0.52 cycles (15.52x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 1.17.2: Binary (unrolled and/or vectorized) loop #2915
==============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:559.

It is main loop of related source loop which is unrolled by 2 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 0.52 cycles (15.52x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.18: Source loop ending at line 564
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2913
and is not unrolled or unrolled with no peel/tail loop.

Section 1.18.1: Binary loop #2913
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:283,564.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 9.00 to 0.56 cycles (16.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 9.00 to 8.40 cycles (1.07x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 1.19: Source loop ending at line 570
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2912
and is not unrolled or unrolled with no peel/tail loop.

Section 1.19.1: Binary loop #2912
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:283,567-570.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 129.25 to 8.14 cycles (15.88x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.20: Source loop ending at line 576
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2911
and is not unrolled or unrolled with no peel/tail loop.

Section 1.20.1: Binary loop #2911
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:574-576.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 6.00 to 0.37 cycles (16.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 6.00 to 5.20 cycles (1.15x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 1.21: Source loop ending at line 616
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2909
and is not unrolled or unrolled with no peel/tail loop.

Section 1.21.1: Binary loop #2909
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:586-616.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=12
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 12 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.01 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 96.40 to 9.50 cycles (10.15x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 96.40 to 6.12 cycles (15.76x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.22: Source loop ending at line 629
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2907
and is not unrolled or unrolled with no peel/tail loop.

Section 1.22.1: Binary loop #2907
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:283,628-629.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 7.60 to 0.64 cycles (11.84x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.23: Source loop ending at line 642
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2906
and is not unrolled or unrolled with no peel/tail loop.

Section 1.23.1: Binary loop #2906
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:283,631-642.

The related source loop is not unrolled or unrolled with no peel/tail loop.
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 1.23.1.1: Path #1
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 33.40 to 2.14 cycles (15.61x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.23.1.2: Path #2
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 18.00 to 1.19 cycles (15.13x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.23.1.3: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 25.70 to 1.66 cycles (15.44x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 1.24: Source loop ending at line 667
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2904
and is not unrolled or unrolled with no peel/tail loop.

Section 1.24.1: Binary loop #2904
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:664-667.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.50 to 0.84 cycles (5.33x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 1.25: Source loop ending at line 691
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2903
and is not unrolled or unrolled with no peel/tail loop.

Section 1.25.1: Binary loop #2903
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:678-691.

The related source loop is not unrolled or unrolled with no peel/tail loop.
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 1.25.1.1: Path #1
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 19.60 to 1.43 cycles (13.71x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.25.1.2: Path #2
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 16.00 to 1.23 cycles (12.96x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.25.1.3: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 17.80 to 1.33 cycles (13.36x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 1.26: Source loop ending at line 706
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2902
and is not unrolled or unrolled with no peel/tail loop.

Section 1.26.1: Binary loop #2902
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:693-706.

The related source loop is not unrolled or unrolled with no peel/tail loop.
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 1.26.1.1: Path #1
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 19.80 to 1.43 cycles (13.85x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.26.1.2: Path #2
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 16.20 to 1.23 cycles (13.12x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.26.1.3: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 18.00 to 1.33 cycles (13.51x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 1.27: Source loop ending at line 719
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2900
and is not unrolled or unrolled with no peel/tail loop.

Section 1.27.1: Binary loop #2900
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:283,716-719.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 108.50 to 6.86 cycles (15.82x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.28: Source loop ending at line 736
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2898
and is not unrolled or unrolled with no peel/tail loop.

Section 1.28.1: Binary loop #2898
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:734-736.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.00 to 0.25 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.29: Source loop ending at line 745
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2897
and is not unrolled or unrolled with no peel/tail loop.

Section 1.29.1: Binary loop #2897
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:743-745.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.40 to 0.50 cycles (8.80x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.30: Source loop ending at line 755
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2896
and is not unrolled or unrolled with no peel/tail loop.

Section 1.30.1: Binary loop #2896
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:283,753-755.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 7.50 to 0.94 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.31: Source loop ending at line 761
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2894
and is not unrolled or unrolled with no peel/tail loop.

Section 1.31.1: Binary loop #2894
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:283,759-761.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 6.00 to 0.75 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 6.00 to 5.20 cycles (1.15x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 1.32: Source loop ending at line 788
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2890
and is not unrolled or unrolled with no peel/tail loop.

Section 1.32.1: Binary loop #2890
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:788.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 4.40 to 1.00 cycles (4.40x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.40 to 0.55 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 1.33: Source loop ending at line 797
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2889
and is not unrolled or unrolled with no peel/tail loop.

Section 1.33.1: Binary loop #2889
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:797.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 5.80 to 0.72 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


All innermost loops were analyzed.

Section 2: Function: nnp_init_subsys
====================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_environment.F

Section 2.1: Source loop ending at line 210
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 2948
and is not unrolled or unrolled with no peel/tail loop.

Section 2.1.1: Binary loop #2948
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment.F:210.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 7.25 to 0.91 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


All innermost loops were analyzed.

[MAQAO] Info: No innermost loops in the function nnp_environment_mp_nnp_init_
[MAQAO] Info: No innermost loops in the function nnp_environment_types._
Section 3: Function: nnp_env_release
====================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_environment_types.F

Section 3.1: Source loop ending at line 286
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 2954
and is not unrolled or unrolled with no peel/tail loop.

Section 3.1.1: Binary loop #2954
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment_types.F:283-286.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 8 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 3.1.1.1: Average path
=============================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 33.00 to 2.44 cycles (13.54x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 3.1.1.2: Path #1
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 37.50 to 2.77 cycles (13.56x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.1.1.3: Path #2
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 35.00 to 2.56 cycles (13.66x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.1.1.4: Path #3
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 34.25 to 2.52 cycles (13.61x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.1.1.5: Path #4
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 31.75 to 2.31 cycles (13.73x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.1.1.6: Path #5
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 34.25 to 2.52 cycles (13.61x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.1.1.7: Path #6
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 31.75 to 2.31 cycles (13.73x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.1.1.8: Path #7
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 31.00 to 2.27 cycles (13.68x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.1.1.9: Path #8
========================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 28.50 to 2.25 cycles (12.67x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.2: Source loop ending at line 308
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 2952
and is not unrolled or unrolled with no peel/tail loop.

Section 3.2.1: Binary loop #2952
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment_types.F:305-308.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 8 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 3.2.1.1: Average path
=============================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 33.00 to 2.44 cycles (13.54x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 3.2.1.2: Path #1
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 37.50 to 2.77 cycles (13.56x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.2.1.3: Path #2
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 35.00 to 2.56 cycles (13.66x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.2.1.4: Path #3
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 34.25 to 2.52 cycles (13.61x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.2.1.5: Path #4
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 31.75 to 2.31 cycles (13.73x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.2.1.6: Path #5
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 34.25 to 2.52 cycles (13.61x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.2.1.7: Path #6
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 31.75 to 2.31 cycles (13.73x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.2.1.8: Path #7
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 31.00 to 2.27 cycles (13.68x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.2.1.9: Path #8
========================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 28.50 to 2.25 cycles (12.67x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 3.3: Source loop ending at line 347
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 2950
and is not unrolled or unrolled with no peel/tail loop.

Section 3.3.1: Binary loop #2950
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment_types.F:332-347.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. If you really need to analyze all of the 243 paths individually, rerun with max-paths=243
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 243 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 86.50 to 2.84 cycles (30.50x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


All innermost loops were analyzed.

Section 4: Function: nnp_env_get
================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_environment_types.F

Section 4.1: Source loop ending at line 448
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2956 (448-448)
 - 2957 (448-448)
 - 2958 (448-448)
 - 2959 (448-448)
 - 2960 (448-448)
 - 2961 (448-448)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 2960
 - unrolled and/or vectorized peel or tail: 2957
 - peel or tail: 2956, 2958, 2959, 2961
The analysis will be displayed for the unrolled and/or vectorized loops: 2960

Section 4.1.1: Binary (unrolled and/or vectorized) loop #2960
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment_types.F:448.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


All innermost loops were analyzed.

Section 5: Function: nnp_env_set
================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_environment_types.F

Section 5.1: Source loop ending at line 533
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2963 (533-533)
 - 2964 (533-533)
 - 2965 (533-533)
 - 2966 (533-533)
 - 2967 (533-533)
 - 2968 (533-533)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 2967
 - unrolled and/or vectorized peel or tail: 2964
 - peel or tail: 2963, 2965, 2966, 2968
The analysis will be displayed for the unrolled and/or vectorized loops: 2967

Section 5.1.1: Binary (unrolled and/or vectorized) loop #2967
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_environment_types.F:533.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


All innermost loops were analyzed.

[MAQAO] Info: No innermost loops in the function nnp_force._
[MAQAO] Info: Assuming lines 200 and 201 correspond to the same source loop
[MAQAO] Info: Assuming lines 101 and 100 correspond to the same source loop
[MAQAO] Info: Assuming lines 102 and 101 correspond to the same source loop
[MAQAO] Info: Assuming lines 200 and 202 correspond to the same source loop
[MAQAO] Info: Assuming lines 212 and 214 correspond to the same source loop
Section 6: Function: nnp_calc_energy_force
==========================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_force.F

Section 6.1: Source loop ending at line 99
==========================================

Composition and unrolling
-------------------------
It is composed of the loop 3045
and is not unrolled or unrolled with no peel/tail loop.

Section 6.1.1: Binary loop #3045
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:99.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 7.25 to 3.80 cycles (1.91x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 7.25 to 0.91 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 6.2: Source loop ending at line 102
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3037 (102-102)
 - 3040 (101-101)
 - 3043 (100-100)
and is multi-versionned.
The analysis will be displayed for all loops

Section 6.2.1: Binary loop #3037
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:102.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.75 to 3.25 cycles (3.31x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 10.75 to 1.34 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.2.2: Binary loop #3040
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:101.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.75 to 3.25 cycles (3.31x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 10.75 to 1.34 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.2.3: Binary loop #3043
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:100.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.75 to 3.25 cycles (3.31x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 10.75 to 1.34 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 6.3: Source loop ending at line 123
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 3034
and is not unrolled or unrolled with no peel/tail loop.

Section 6.3.1: Binary loop #3034
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:107-123.

The related source loop is not unrolled or unrolled with no peel/tail loop.
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 6.3.1.1: Path #1
========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 25.50 to 1.59 cycles (16.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 25.50 to 23.40 cycles (1.09x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 6.3.1.2: Path #2
========================

Vectorization
-------------
Your loop is not vectorized.
Only 5% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 6.00 to 0.13 cycles (47.54x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 6.00 to 5.00 cycles (1.20x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 6.3.1.3: Average path
=============================

Vectorization
-------------
Your loop is not vectorized.
Only 7% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 15.75 to 0.86 cycles (18.31x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 6.4: Source loop ending at line 127
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3031 (127-127)
 - 3032 (127-127)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3032
 - peel or tail: 3031
The analysis will be displayed for the unrolled and/or vectorized loops: 3032

Section 6.4.1: Binary (unrolled and/or vectorized) loop #3032
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:127.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 6.5: Source loop ending at line 131
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3028 (130-131)
 - 3029 (130-131)
 - 3030 (130-131)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3029
 - peel or tail: 3028, 3030
The analysis will be displayed for the unrolled and/or vectorized loops: 3029

Section 6.5.1: Binary (unrolled and/or vectorized) loop #3029
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:130-131.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 6.6: Source loop ending at line 144
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 3027
and is not unrolled or unrolled with no peel/tail loop.

Section 6.6.1: Binary loop #3027
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:144.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.50 to 2.00 cycles (5.25x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 10.50 to 1.31 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 10.50 to 9.60 cycles (1.09x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 6.7: Source loop ending at line 149
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 3026
and is not unrolled or unrolled with no peel/tail loop.

Section 6.7.1: Binary loop #3026
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:149.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.50 to 3.00 cycles (3.50x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 10.50 to 1.31 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 10.50 to 10.00 cycles (1.05x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 6.8: Source loop ending at line 151
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 3025
and is not unrolled or unrolled with no peel/tail loop.

Section 6.8.1: Binary loop #3025
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:151.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 3.00 to 0.75 cycles (4.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 6.9: Source loop ending at line 154
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3022 (154-154)
 - 3023 (154-154)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3023
 - peel or tail: 3022
The analysis will be displayed for the unrolled and/or vectorized loops: 3023

Section 6.9.1: Binary (unrolled and/or vectorized) loop #3023
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:154.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 128 out of 512 bits (SSE/AVX-128 instructions on AVX-512 processors).
By fully vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.50 cycles (2.00x speedup).
All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).
Since your execution units are vector units, only a fully vectorized loop can use their full power.

Workaround(s):
 - Pass to your compiler a micro-architecture specialization option:
  * use axHost or xHost
 - Use vector aligned instructions:
  1) align your arrays on 64 bytes boundaries: compile with -align array64byte (remark: not affects arrays in COMMON blocks).
  2) inform your compiler that your arrays are vector aligned: Append !DIR$ VECTOR ALIGNED to the loop if all accessed arrays are aligned, or !DIR$ ASSUME_ALIGNED FOO: 64 if only FOO is aligned.
 - Use the LOOP COUNT directive


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 6.10: Source loop ending at line 172
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3020 (172-172)
 - 3021 (172-172)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3021
 - peel or tail: 3020
The analysis will be displayed for the unrolled and/or vectorized loops: 3021

Section 6.10.1: Binary (unrolled and/or vectorized) loop #3021
==============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:172.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 128 out of 512 bits (SSE/AVX-128 instructions on AVX-512 processors).
By fully vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.50 cycles (2.00x speedup).
All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).
Since your execution units are vector units, only a fully vectorized loop can use their full power.

Workaround(s):
 - Pass to your compiler a micro-architecture specialization option:
  * use axHost or xHost
 - Use vector aligned instructions:
  1) align your arrays on 64 bytes boundaries: compile with -align array64byte (remark: not affects arrays in COMMON blocks).
  2) inform your compiler that your arrays are vector aligned: Append !DIR$ VECTOR ALIGNED to the loop if all accessed arrays are aligned, or !DIR$ ASSUME_ALIGNED FOO: 64 if only FOO is aligned.
 - Use the LOOP COUNT directive


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 6.11: Source loop ending at line 177
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3019
and is not unrolled or unrolled with no peel/tail loop.

Section 6.11.1: Binary loop #3019
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:87,175-177.

The related source loop is not unrolled or unrolled with no peel/tail loop.
1% of peak computational performance is used (0.44 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 13.50 to 3.00 cycles (4.50x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 13.50 to 1.69 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by:
 - execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck)
 - reading data from caches/RAM (load units are a bottleneck)

By removing all these bottlenecks, you can lower the cost of an iteration from 13.50 to 11.80 cycles (1.14x speedup).

Workaround(s):
 - Reduce the number of FP multiply/FMA instructions
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


FMA
---
Detected 3 FMA (fused multiply-add) operations.

Section 6.12: Source loop ending at line 182
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3018
and is not unrolled or unrolled with no peel/tail loop.

Section 6.12.1: Binary loop #3018
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:181-182.

The related source loop is not unrolled or unrolled with no peel/tail loop.
1% of peak computational performance is used (0.40 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.00 to 7.00 cycles (1.43x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 10.00 to 1.25 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


FMA
---
Detected 2 FMA (fused multiply-add) operations.

Section 6.13: Source loop ending at line 200
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2996 (202-202)
 - 2997 (202-202)
 - 2998 (202-202)
 - 2999 (202-202)
 - 3000 (201-201)
 - 3002 (201-201)
 - 3003 (201-201)
 - 3004 (201-201)
 - 3005 (201-201)
 - 3006 (201-201)
 - 3007 (201-201)
 - 3009 (200-200)
 - 3011 (200-200)
 - 3012 (200-200)
 - 3013 (200-200)
and is multi-versionned.
The analysis will be displayed for all loops

Section 6.13.1: Binary loop #2996
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:202.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.13.2: Binary loop #2997
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:202.

The related source loop is multi-versionned.
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.13.3: Binary loop #2998
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:202.

The related source loop is multi-versionned.
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.13.4: Binary loop #2999
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:202.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.13.5: Binary loop #3000
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:201.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 5.20 to 0.65 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.13.6: Binary loop #3002
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:201.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.13.7: Binary loop #3003
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:201.

The related source loop is multi-versionned.
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.13.8: Binary loop #3004
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:201.

The related source loop is multi-versionned.
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.13.9: Binary loop #3005
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:201.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.13.10: Binary loop #3006
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:201.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.13.11: Binary loop #3007
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:201.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.13.12: Binary loop #3009
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:200.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 7.25 to 0.91 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.13.13: Binary loop #3011
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:200.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.13.14: Binary loop #3012
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:200.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.13.15: Binary loop #3013
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:200.

The related source loop is multi-versionned.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 13% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.20 to 0.56 cycles (7.56x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 6.14: Source loop ending at line 208
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2995
and is not unrolled or unrolled with no peel/tail loop.

Section 6.14.1: Binary loop #2995
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:208.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 9.75 to 1.22 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 6.15: Source loop ending at line 212
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2980 (213-214)
 - 2982 (212-212)
 - 2985 (212-212)
 - 2986 (212-212)
 - 2987 (212-212)
 - 2988 (212-212)
 - 2990 (212-212)
 - 2991 (212-212)
 - 2992 (212-212)
and is multi-versionned.
The analysis will be displayed for all loops

Section 6.15.1: Binary loop #2980
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:213-214.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 9.00 to 1.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 9.00 to 7.40 cycles (1.22x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop



Section 6.15.2: Binary loop #2982
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:212.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.00 to 4.00 cycles (2.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 8.00 to 7.40 cycles (1.08x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 8.00 to 7.40 cycles (1.08x speedup).


Section 6.15.3: Binary loop #2985
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:212.

The related source loop is multi-versionned.
2% of peak computational performance is used (0.83 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.20 to 0.15 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.15.4: Binary loop #2986
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:212.

The related source loop is multi-versionned.
10% of peak computational performance is used (3.33 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.15.5: Binary loop #2987
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:212.

The related source loop is multi-versionned.
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 3.80 cycles (1.05x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.15.6: Binary loop #2988
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:212.

The related source loop is multi-versionned.
2% of peak computational performance is used (0.83 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.20 to 0.15 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.15.7: Binary loop #2990
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:212.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.15.8: Binary loop #2991
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:212.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.15.9: Binary loop #2992
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:212.

The related source loop is multi-versionned.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 13% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.80 to 0.27 cycles (10.35x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 6.16: Source loop ending at line 220
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 2972 (220-220)
 - 2973 (220-220)
 - 2974 (220-220)
 - 2975 (220-220)
 - 2977 (220-220)
 - 2978 (220-220)
 - 2979 (220-220)
and is multi-versionned.
The analysis will be displayed for all loops

Section 6.16.1: Binary loop #2972
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:220.

The related source loop is multi-versionned.
2% of peak computational performance is used (0.83 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.20 to 0.15 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.16.2: Binary loop #2973
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:220.

The related source loop is multi-versionned.
10% of peak computational performance is used (3.33 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.16.3: Binary loop #2974
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:220.

The related source loop is multi-versionned.
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 3.80 cycles (1.05x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.16.4: Binary loop #2975
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:220.

The related source loop is multi-versionned.
2% of peak computational performance is used (0.83 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.20 to 0.15 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.16.5: Binary loop #2977
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:220.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 6.16.6: Binary loop #2978
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:220.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 6.16.7: Binary loop #2979
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:220.

The related source loop is multi-versionned.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 13% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.80 to 0.27 cycles (10.35x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 6.17: Source loop ending at line 229
============================================

Composition and unrolling
-------------------------
It is composed of the loop 2969
and is not unrolled or unrolled with no peel/tail loop.

Section 6.17.1: Binary loop #2969
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:87,228-229.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.29 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.50 to 3.00 cycles (3.50x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 10.50 to 1.31 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 10.50 to 9.00 cycles (1.17x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop



All innermost loops were analyzed.

[MAQAO] Info: Assuming lines 279 and 281 correspond to the same source loop
[MAQAO] Info: Assuming lines 296 and 298 correspond to the same source loop
Section 7: Function: nnp_bias_sigma
===================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_force.F

Section 7.1: Source loop ending at line 268
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3073 (268-268)
 - 3074 (268-268)
 - 3075 (268-268)
and is multi-versionned.
The analysis will be displayed for all loops

Section 7.1.1: Binary loop #3073
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:268.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 7.1.2: Binary loop #3074
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:268.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 7.1.3: Binary loop #3075
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:268.

The related source loop is multi-versionned.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 13% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 3.80 to 0.42 cycles (9.14x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 7.2: Source loop ending at line 273
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3068 (273-273)
 - 3069 (273-273)
 - 3070 (273-273)
 - 3071 (273-273)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3070
 - unrolled and/or vectorized peel or tail: 3069
 - peel or tail: 3068, 3071
The analysis will be displayed for the unrolled and/or vectorized loops: 3070

Section 7.2.1: Binary (unrolled and/or vectorized) loop #3070
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:273.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 3.60 cycles (1.11x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 7.3: Source loop ending at line 279
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3061 (280-281)
 - 3062 (280-281)
 - 3063 (280-281)
 - 3064 (279-279)
 - 3065 (279-279)
 - 3066 (279-279)
 - 3067 (279-279)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3062, 3066
 - unrolled and/or vectorized peel or tail: 3061, 3063, 3065
 - peel or tail: 3064, 3067
The analysis will be displayed for the unrolled and/or vectorized loops: 3062, 3066

Section 7.3.1: Binary (unrolled and/or vectorized) loop #3062
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:280-281.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
37% of peak computational performance is used (12.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 2.80 cycles (1.43x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

FMA
---
Detected 16 FMA (fused multiply-add) operations.


Section 7.3.2: Binary (unrolled and/or vectorized) loop #3066
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:279.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 7.4: Source loop ending at line 296
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3054 (296-296)
 - 3055 (296-296)
 - 3056 (296-296)
 - 3057 (296-298)
 - 3058 (296-298)
 - 3059 (296-298)
 - 3060 (296-298)
and is multi-versionned.
The analysis will be displayed for all loops

Section 7.4.1: Binary loop #3054
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:296.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 7.4.2: Binary loop #3055
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:296.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 7.4.3: Binary loop #3056
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:296.

The related source loop is multi-versionned.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 14% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 3.60 to 0.48 cycles (7.47x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 7.4.4: Binary loop #3057
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:296-298.

The related source loop is multi-versionned.
5% of peak computational performance is used (1.88 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.60 to 0.20 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 1 FMA (fused multiply-add) operations.


Section 7.4.5: Binary loop #3058
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:296-298.

The related source loop is multi-versionned.
23% of peak computational performance is used (7.50 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

FMA
---
Detected 4 FMA (fused multiply-add) operations.


Section 7.4.6: Binary loop #3059
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:296-298.

The related source loop is multi-versionned.
25% of peak computational performance is used (8.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 6.00 to 5.40 cycles (1.11x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

FMA
---
Detected 16 FMA (fused multiply-add) operations.


Section 7.4.7: Binary loop #3060
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:296-298.

The related source loop is multi-versionned.
5% of peak computational performance is used (1.88 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.60 to 0.20 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 1 FMA (fused multiply-add) operations.

Section 7.5: Source loop ending at line 301
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3048 (301-301)
 - 3049 (301-301)
 - 3050 (301-301)
 - 3051 (301-301)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3050
 - unrolled and/or vectorized peel or tail: 3049
 - peel or tail: 3048, 3051
The analysis will be displayed for the unrolled and/or vectorized loops: 3050

Section 7.5.1: Binary (unrolled and/or vectorized) loop #3050
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:301.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
17% of peak computational performance is used (5.71 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


All innermost loops were analyzed.

[MAQAO] Info: No innermost loops in the function nnp_force_mp_nnp_print_bias_
Section 8: Function: nnp_print_bias_energy
==========================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_force.F

Section 8.1: Source loop ending at line 591
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3076 (585-591)
 - 3077 (585-591)
and is multi-versionned.
The analysis will be displayed for all loops

Section 8.1.1: Binary loop #3076
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:585-591.

The related source loop is multi-versionned.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.40 to 0.56 cycles (7.82x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 8.1.2: Binary loop #3077
================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:585-591.

The related source loop is multi-versionned.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.40 to 0.56 cycles (7.82x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


All innermost loops were analyzed.

Section 9: Function: nnp_print_bias_forces
==========================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_force.F

Section 9.1: Source loop ending at line 621
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3079 (621-621)
 - 3080 (621-621)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3080
 - peel or tail: 3079
The analysis will be displayed for the unrolled and/or vectorized loops: 3080

Section 9.1.1: Binary (unrolled and/or vectorized) loop #3080
=============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:621.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


All innermost loops were analyzed.

[MAQAO] Info: No innermost loops in the function nnp_force_mp_nnp_print_
Section 10: Function: nnp_print_energies
========================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_force.F

Section 10.1: Source loop ending at line 398
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3100
and is not unrolled or unrolled with no peel/tail loop.

Section 10.1.1: Binary loop #3100
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:397-398.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.40 to 0.56 cycles (7.82x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 10.2: Source loop ending at line 404
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3090 (404-404)
 - 3091 (404-404)
 - 3092 (404-404)
 - 3094 (404-404)
 - 3095 (404-404)
 - 3096 (404-404)
 - 3097 (404-404)
 - 3098 (404-404)
 - 3099 (404-404)
and is multi-versionned.
The analysis will be displayed for all loops

Section 10.2.1: Binary loop #3090
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:404.

The related source loop is multi-versionned.
9% of peak computational performance is used (3.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Masked instructions
-------------------
Detected masked instructions.
Vector registers are partially exploited, which is expected if your loop is irregular or mixes elements of different sizes.
Workaround(s):
If your loop is irregular, try to remove or hoist conditional structures out of your loop. If it mixes elements of different sizes, try to uniformize them.

FMA
---
Presence of both ADD/SUB and MUL operations.
Workaround(s):
 - Pass to your compiler a micro-architecture specialization option:
  * use axHost or xHost
 - Try to change order in which elements are evaluated (using parentheses) in arithmetic expressions containing both ADD/SUB and MUL operations to enable your compiler to generate FMA instructions wherever possible.
For instance a + b*c is a valid FMA (MUL then ADD).
However (a+b)* c cannot be translated into an FMA (ADD then MUL).



Section 10.2.2: Binary loop #3091
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:404.

The related source loop is multi-versionned.
37% of peak computational performance is used (12.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 2.80 cycles (1.43x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

FMA
---
Detected 16 FMA (fused multiply-add) operations.


Section 10.2.3: Binary loop #3092
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:404.

The related source loop is multi-versionned.
9% of peak computational performance is used (3.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Masked instructions
-------------------
Detected masked instructions.
Vector registers are partially exploited, which is expected if your loop is irregular or mixes elements of different sizes.
Workaround(s):
If your loop is irregular, try to remove or hoist conditional structures out of your loop. If it mixes elements of different sizes, try to uniformize them.

FMA
---
Presence of both ADD/SUB and MUL operations.
Workaround(s):
 - Pass to your compiler a micro-architecture specialization option:
  * use axHost or xHost
 - Try to change order in which elements are evaluated (using parentheses) in arithmetic expressions containing both ADD/SUB and MUL operations to enable your compiler to generate FMA instructions wherever possible.
For instance a + b*c is a valid FMA (MUL then ADD).
However (a+b)* c cannot be translated into an FMA (ADD then MUL).



Section 10.2.4: Binary loop #3094
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:404.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 10.2.5: Binary loop #3095
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:404.

The related source loop is multi-versionned.
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 10.2.6: Binary loop #3096
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:404.

The related source loop is multi-versionned.
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 10.2.7: Binary loop #3097
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:404.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 10.2.8: Binary loop #3098
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:404.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 10.2.9: Binary loop #3099
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:404.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 10.3: Source loop ending at line 407
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3081 (407-407)
 - 3082 (407-407)
 - 3084 (407-407)
 - 3085 (407-407)
 - 3086 (407-407)
 - 3087 (407-407)
 - 3088 (407-407)
 - 3089 (407-407)
and is multi-versionned.
The analysis will be displayed for all loops

Section 10.3.1: Binary loop #3081
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:407.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 10.3.2: Binary loop #3082
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:407.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 10.3.3: Binary loop #3084
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:407.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 10.3.4: Binary loop #3085
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:407.

The related source loop is multi-versionned.
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 10.3.5: Binary loop #3086
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:407.

The related source loop is multi-versionned.
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 10.3.6: Binary loop #3087
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:407.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 10.3.7: Binary loop #3088
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:407.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.06 cycles (16.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 10.3.8: Binary loop #3089
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:407.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


All innermost loops were analyzed.

Section 11: Function: nnp_print_forces
======================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_force.F

Section 11.1: Source loop ending at line 442
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3103 (442-442)
 - 3104 (442-442)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3104
 - peel or tail: 3103
The analysis will be displayed for the unrolled and/or vectorized loops: 3104

Section 11.1.1: Binary (unrolled and/or vectorized) loop #3104
==============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:442.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


All innermost loops were analyzed.

Section 12: Function: nnp_print_force_sigma
===========================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_force.F

Section 12.1: Source loop ending at line 471
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3105
and is not unrolled or unrolled with no peel/tail loop.

Section 12.1.1: Binary loop #3105
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:470-471.

The related source loop is not unrolled or unrolled with no peel/tail loop.
7% of peak computational performance is used (2.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 3 FMA (fused multiply-add) operations.


All innermost loops were analyzed.

Section 13: Function: nnp_print_expol
=====================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_force.F

Section 13.1: Source loop ending at line 510
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3112
and is not unrolled or unrolled with no peel/tail loop.

Section 13.1.1: Binary loop #3112
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:505-510.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
2% of peak computational performance is used (0.85 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.20 to 4.00 cycles (2.05x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 8.20 to 0.84 cycles (9.72x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 3 FMA (fused multiply-add) operations.

Section 13.2: Source loop ending at line 513
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3108 (513-513)
 - 3110 (513-513)
and is unrolled by 2 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3110
 - peel or tail: 3108
The analysis will be displayed for the unrolled and/or vectorized loops: 3110

Section 13.2.1: Binary (unrolled and/or vectorized) loop #3110
==============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:513.

It is main loop of related source loop which is unrolled by 2 (including vectorization).
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.00 to 1.80 cycles (1.11x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is poorly vectorized.
Only 15% of vector register length is used (average across all SSE/AVX instructions).
By fully vectorizing your loop, you can lower the cost of an iteration from 2.00 to 0.28 cycles (7.20x speedup).
20% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 33% of SSE/AVX loads are used in vector version.
 - 0% of SSE/AVX stores are used in vector version.
Since your execution units are vector units, only a fully vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 13.3: Source loop ending at line 522
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3107
and is not unrolled or unrolled with no peel/tail loop.

Section 13.3.1: Binary loop #3107
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:519-522.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.29 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.20 to 4.00 cycles (2.55x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 10.20 to 1.12 cycles (9.07x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


All innermost loops were analyzed.

Section 14: Function: nnp_print_sumforces
=========================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_force.F

Section 14.1: Source loop ending at line 663
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3114
and is not unrolled or unrolled with no peel/tail loop.

Section 14.1.1: Binary loop #3114
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_force.F:660-663.

The related source loop is not unrolled or unrolled with no peel/tail loop.
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.21 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 14.1.1.1: Path #1
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 14.00 to 4.00 cycles (3.50x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 14.00 to 1.14 cycles (12.24x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 14.1.1.2: Path #2
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 10.50 to 0.75 cycles (14.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 14.1.1.3: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 12.25 to 0.95 cycles (12.93x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)



All innermost loops were analyzed.

[MAQAO] Info: No innermost loops in the function nnp_model._
Section 15: Function: nnp_write_arc
===================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_model.F

Section 15.1: Source loop ending at line 67
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 3116
and is not unrolled or unrolled with no peel/tail loop.

Section 15.1.1: Binary loop #3116
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:65-67.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 9.40 to 0.78 cycles (12.03x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


All innermost loops were analyzed.

Section 16: Function: nnp_predict
=================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_model.F

Section 16.1: Source loop ending at line 95
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3130 (95-95)
 - 3131 (95-95)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3131
 - peel or tail: 3130
The analysis will be displayed for the unrolled and/or vectorized loops: 3131

Section 16.1.1: Binary (unrolled and/or vectorized) loop #3131
==============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:95.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 16.2: Source loop ending at line 105
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3125 (105-105)
 - 3126 (105-105)
 - 3128 (105-105)
 - 3129 (105-105)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3126, 3129
 - peel or tail: 3125, 3128
The analysis will be displayed for the unrolled and/or vectorized loops: 3126, 3129

Section 16.2.1: Binary (unrolled and/or vectorized) loop #3126
==============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:105.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 16.2.2: Binary (unrolled and/or vectorized) loop #3129
==============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:105.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 16.3: Source loop ending at line 115
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3123
and is not unrolled or unrolled with no peel/tail loop.

Section 16.3.1: Binary loop #3123
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:114-115.

The related source loop is not unrolled or unrolled with no peel/tail loop.
2% of peak computational performance is used (0.91 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.20 to 2.00 cycles (1.10x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.20 to 0.28 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 16.4: Source loop ending at line 122
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3119 (121-122)
 - 3120 (121-122)
 - 3121 (121-122)
 - 3122 (121-122)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3121
 - unrolled and/or vectorized peel or tail: 3120
 - peel or tail: 3119, 3122
The analysis will be displayed for the unrolled and/or vectorized loops: 3121

Section 16.4.1: Binary (unrolled and/or vectorized) loop #3121
==============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:121-122.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
17% of peak computational performance is used (5.71 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 16.5: Source loop ending at line 129
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3118
and is not unrolled or unrolled with no peel/tail loop.

Section 16.5.1: Binary loop #3118
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:128-129.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.40 to 0.25 cycles (5.60x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 16.6: Source loop ending at line 135
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3132
and is not unrolled or unrolled with no peel/tail loop.

Section 16.6.1: Binary loop #3132
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:135.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.20 to 1.60 cycles (1.37x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 16% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.20 to 0.31 cycles (7.11x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 16.7: Source loop ending at line 137
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3133
and is not unrolled or unrolled with no peel/tail loop.

Section 16.7.1: Binary loop #3133
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:137.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
2% of peak computational performance is used (0.71 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.80 to 2.20 cycles (1.27x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 16% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.80 to 0.41 cycles (6.77x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 16.8: Source loop ending at line 141
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3134
and is not unrolled or unrolled with no peel/tail loop.

Section 16.8.1: Binary loop #3134
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:141.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.20 to 1.60 cycles (1.37x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 16% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.20 to 0.31 cycles (7.11x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 16.9: Source loop ending at line 143
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3135
and is not unrolled or unrolled with no peel/tail loop.

Section 16.9.1: Binary loop #3135
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:143.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
1% of peak computational performance is used (0.50 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 16% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 2.00 cycles (2.00x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 3.00 cycles (1.33x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 4.00 to 3.00 cycles (1.33x speedup).

Section 16.10: Source loop ending at line 145
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 3136
and is not unrolled or unrolled with no peel/tail loop.

Section 16.10.1: Binary loop #3136
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:145.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
2% of peak computational performance is used (0.75 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 15% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 2.00 cycles (2.00x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 3.20 cycles (1.25x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 4.00 to 3.20 cycles (1.25x speedup).

Section 16.11: Source loop ending at line 147
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 3137
and is not unrolled or unrolled with no peel/tail loop.

Section 16.11.1: Binary loop #3137
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:147.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.40 to 1.80 cycles (1.33x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 18% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.40 to 0.37 cycles (6.55x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 16.12: Source loop ending at line 149
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 3138
and is not unrolled or unrolled with no peel/tail loop.

Section 16.12.1: Binary loop #3138
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:149.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
1% of peak computational performance is used (0.36 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.80 to 1.80 cycles (1.56x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 15% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.80 to 0.38 cycles (7.27x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 16.13: Source loop ending at line 151
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 3139
and is not unrolled or unrolled with no peel/tail loop.

Section 16.13.1: Binary loop #3139
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:151.

The related source loop is not unrolled or unrolled with no peel/tail loop.
3% of peak computational performance is used (1.11 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 1.80 to 1.60 cycles (1.12x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.80 to 0.25 cycles (7.20x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


All innermost loops were analyzed.

[MAQAO] Info: Assuming lines 192 and 194 correspond to the same source loop
Section 17: Function: nnp_gradients
===================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_model.F

Section 17.1: Source loop ending at line 188
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3158
and is not unrolled or unrolled with no peel/tail loop.

Section 17.1.1: Binary loop #3158
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:188.

The related source loop is not unrolled or unrolled with no peel/tail loop.
6% of peak computational performance is used (2.22 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 1.80 to 1.60 cycles (1.12x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.80 to 0.25 cycles (7.20x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 2 FMA (fused multiply-add) operations.

Section 17.2: Source loop ending at line 190
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3159
and is not unrolled or unrolled with no peel/tail loop.

Section 17.2.1: Binary loop #3159
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:190.

The related source loop is not unrolled or unrolled with no peel/tail loop.
2% of peak computational performance is used (0.67 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 15% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 3.00 to 0.50 cycles (6.00x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 3.00 to 2.60 cycles (1.15x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 17.3: Source loop ending at line 192
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3160 (192-192)
 - 3161 (192-192)
 - 3162 (192-192)
 - 3163 (192-192)
 - 3164 (194-194)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3162
 - unrolled and/or vectorized peel or tail: 3161
 - peel or tail: 3160, 3163, 3164
The analysis will be displayed for the unrolled and/or vectorized loops: 3162

Section 17.3.1: Binary (unrolled and/or vectorized) loop #3162
==============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:192.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 17.4: Source loop ending at line 197
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3165
and is not unrolled or unrolled with no peel/tail loop.

Section 17.4.1: Binary loop #3165
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:196-197.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
2% of peak computational performance is used (0.71 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 4.20 to 4.00 cycles (1.05x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 15% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.20 to 2.00 cycles (2.10x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Presence of both ADD/SUB and MUL operations.
Workaround(s):
 - Pass to your compiler a micro-architecture specialization option:
  * use axHost or xHost
 - Try to change order in which elements are evaluated (using parentheses) in arithmetic expressions containing both ADD/SUB and MUL operations to enable your compiler to generate FMA instructions wherever possible.
For instance a + b*c is a valid FMA (MUL then ADD).
However (a+b)* c cannot be translated into an FMA (ADD then MUL).


Section 17.5: Source loop ending at line 200
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3166
and is not unrolled or unrolled with no peel/tail loop.

Section 17.5.1: Binary loop #3166
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:199-200.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
2% of peak computational performance is used (0.68 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 4.40 to 4.00 cycles (1.10x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 16% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.40 to 2.00 cycles (2.20x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Presence of both ADD/SUB and MUL operations.
Workaround(s):
 - Pass to your compiler a micro-architecture specialization option:
  * use axHost or xHost
 - Try to change order in which elements are evaluated (using parentheses) in arithmetic expressions containing both ADD/SUB and MUL operations to enable your compiler to generate FMA instructions wherever possible.
For instance a + b*c is a valid FMA (MUL then ADD).
However (a+b)* c cannot be translated into an FMA (ADD then MUL).


Section 17.6: Source loop ending at line 202
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3167
and is not unrolled or unrolled with no peel/tail loop.

Section 17.6.1: Binary loop #3167
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:202.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 16% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.00 to 0.37 cycles (5.33x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 2.00 to 1.80 cycles (1.11x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 17.7: Source loop ending at line 205
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3168
and is not unrolled or unrolled with no peel/tail loop.

Section 17.7.1: Binary loop #3168
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:204-205.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
1% of peak computational performance is used (0.50 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 15% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 2.00 cycles (2.00x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 2.50 cycles (1.60x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


Section 17.8: Source loop ending at line 207
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3169
and is not unrolled or unrolled with no peel/tail loop.

Section 17.8.1: Binary loop #3169
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:207.

The related source loop is not unrolled or unrolled with no peel/tail loop.
3% of peak computational performance is used (1.11 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 1.80 to 1.60 cycles (1.12x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.80 to 0.25 cycles (7.20x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 17.9: Source loop ending at line 214
============================================

Composition and unrolling
-------------------------
It is composed of the loop 3157
and is not unrolled or unrolled with no peel/tail loop.

Section 17.9.1: Binary loop #3157
=================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:214.

The related source loop is not unrolled or unrolled with no peel/tail loop.
3% of peak computational performance is used (1.11 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 1.80 to 1.60 cycles (1.12x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.80 to 0.25 cycles (7.20x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 17.10: Source loop ending at line 222
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 3156
and is not unrolled or unrolled with no peel/tail loop.

Section 17.10.1: Binary loop #3156
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:221-222.

The related source loop is not unrolled or unrolled with no peel/tail loop.
2% of peak computational performance is used (0.83 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.20 to 0.15 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 17.11: Source loop ending at line 228
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3153 (228-228)
 - 3154 (228-228)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3154
 - peel or tail: 3153
The analysis will be displayed for the unrolled and/or vectorized loops: 3154

Section 17.11.1: Binary (unrolled and/or vectorized) loop #3154
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:228.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 17.12: Source loop ending at line 242
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3147 (242-242)
 - 3148 (242-242)
 - 3150 (242-242)
 - 3151 (242-242)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3148, 3151
 - peel or tail: 3147, 3150
The analysis will be displayed for the unrolled and/or vectorized loops: 3148, 3151

Section 17.12.1: Binary (unrolled and/or vectorized) loop #3148
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:242.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 17.12.2: Binary (unrolled and/or vectorized) loop #3151
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:242.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 17.13: Source loop ending at line 252
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 3145
and is not unrolled or unrolled with no peel/tail loop.

Section 17.13.1: Binary loop #3145
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:251-252.

The related source loop is not unrolled or unrolled with no peel/tail loop.
2% of peak computational performance is used (0.91 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.20 to 2.00 cycles (1.10x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.20 to 0.28 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 17.14: Source loop ending at line 259
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 3141 (258-259)
 - 3142 (258-259)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 3142
 - peel or tail: 3141
The analysis will be displayed for the unrolled and/or vectorized loops: 3142

Section 17.14.1: Binary (unrolled and/or vectorized) loop #3142
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_model.F:258-259.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


All innermost loops were analyzed.

[MAQAO] Info: No innermost loops in the function nnp_acsf._
[MAQAO] Info: Assuming lines 87 and 86 correspond to the same source loop
Section 18: Function: nnp_calc_acsf
===================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 18.1: Source loop ending at line 58
===========================================

Composition and unrolling
-------------------------
It is composed of the loop 58130
and is not unrolled or unrolled with no peel/tail loop.

Section 18.1.1: Binary loop #58130
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:58.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by:
 - reading data from caches/RAM (load units are a bottleneck)
 - writing data to caches/RAM (the store unit is a bottleneck)

Workaround(s):
 - Read less array elements
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 18.2: Source loop ending at line 87
===========================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58157 (87-87)
 - 58158 (86-86)
and is multi-versionned.
The analysis will be displayed for all loops

Section 18.2.1: Binary loop #58157
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:87.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 7.00 to 0.87 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 7.00 to 6.60 cycles (1.06x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop



Section 18.2.2: Binary loop #58158
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:86.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 7.60 to 0.95 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 18.3: Source loop ending at line 114
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58156
and is not unrolled or unrolled with no peel/tail loop.

Section 18.3.1: Binary loop #58156
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:79,103-114.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 3 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

1% of peak computational performance is used (0.48 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 18.3.1.1: Path #1
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 14.50 to 6.50 cycles (2.23x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 14.50 to 1.81 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 14.50 to 11.00 cycles (1.32x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 18.3.1.2: Path #2
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 24.00 to 12.50 cycles (1.92x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 24.00 to 3.00 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 24.00 to 20.60 cycles (1.17x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


FMA
---
Detected 9 FMA (fused multiply-add) operations.

Section 18.3.1.3: Path #3
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 25.00 to 12.50 cycles (2.00x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 25.00 to 3.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 25.00 to 23.00 cycles (1.09x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


FMA
---
Detected 9 FMA (fused multiply-add) operations.

Section 18.3.1.4: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 21.17 to 2.65 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


FMA
---
Detected 6 FMA (fused multiply-add) operations.

Section 18.4: Source loop ending at line 156
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58151 (155-156)
 - 58152 (155-156)
and is unrolled by 3 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58151
 - peel or tail: 58152
The analysis will be displayed for the unrolled and/or vectorized loops: 58151

Section 18.4.1: Binary (unrolled and/or vectorized) loop #58151
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:155-156.

It is main loop of related source loop which is unrolled by 3 (including vectorization).
11% of peak computational performance is used (3.75 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 3.20 to 2.60 cycles (1.23x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is poorly vectorized.
Only 15% of vector register length is used (average across all SSE/AVX instructions).
By fully vectorizing your loop, you can lower the cost of an iteration from 3.20 to 0.47 cycles (6.80x speedup).
27% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 25% of SSE/AVX loads are used in vector version.
 - 50% of SSE/AVX stores are used in vector version.
 - 50% of SSE/AVX fused multiply-add instructions are used in vector version.
 - 0% of SSE/AVX instructions that are not load, store, addition, subtraction nor multiply instructions are used in vector version.
Since your execution units are vector units, only a fully vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 6 FMA (fused multiply-add) operations.

Section 18.5: Source loop ending at line 195
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58145 (194-195)
 - 58146 (194-195)
and is unrolled by 3 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58145
 - peel or tail: 58146
The analysis will be displayed for the unrolled and/or vectorized loops: 58145

Section 18.5.1: Binary (unrolled and/or vectorized) loop #58145
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:194-195.

It is main loop of related source loop which is unrolled by 3 (including vectorization).
11% of peak computational performance is used (3.75 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 3.20 to 2.60 cycles (1.23x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is poorly vectorized.
Only 15% of vector register length is used (average across all SSE/AVX instructions).
By fully vectorizing your loop, you can lower the cost of an iteration from 3.20 to 0.47 cycles (6.80x speedup).
27% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 25% of SSE/AVX loads are used in vector version.
 - 50% of SSE/AVX stores are used in vector version.
 - 50% of SSE/AVX fused multiply-add instructions are used in vector version.
 - 0% of SSE/AVX instructions that are not load, store, addition, subtraction nor multiply instructions are used in vector version.
Since your execution units are vector units, only a fully vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 6 FMA (fused multiply-add) operations.

Section 18.6: Source loop ending at line 221
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58140
and is not unrolled or unrolled with no peel/tail loop.

Section 18.6.1: Binary loop #58140
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:79,220-221.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.15 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 6.50 to 1.50 cycles (4.33x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 6.50 to 0.81 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 6.50 to 5.20 cycles (1.25x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 18.7: Source loop ending at line 246
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58137
and is not unrolled or unrolled with no peel/tail loop.

Section 18.7.1: Binary loop #58137
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:79,245-246.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.15 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 6.50 to 1.40 cycles (4.64x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 6.50 to 0.81 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 6.50 to 5.80 cycles (1.12x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 18.8: Source loop ending at line 265
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58134
and is not unrolled or unrolled with no peel/tail loop.

Section 18.8.1: Binary loop #58134
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:79,264-265.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.15 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 6.50 to 1.40 cycles (4.64x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 6.50 to 0.81 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 6.50 to 5.80 cycles (1.12x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop



All innermost loops were analyzed.

Section 19: Function: nnp_calc_ang
==================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 19.1: Source loop ending at line 675
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58161 (675-675)
 - 58162 (675-675)
 - 58163 (675-675)
 - 58164 (675-675)
 - 58166 (675-675)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58163
 - unrolled and/or vectorized peel or tail: 58162, 58166
 - peel or tail: 58161, 58164
The analysis will be displayed for the unrolled and/or vectorized loops: 58163

Section 19.1.1: Binary (unrolled and/or vectorized) loop #58163
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:675.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


All innermost loops were analyzed.

Section 20: Function: nnp_calc_rad
==================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 20.1: Source loop ending at line 559
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58167 (543-559)
 - 58168 (543-559)
 - 58169 (543-559)
 - 58170 (543-559)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58170
 - unrolled and/or vectorized peel or tail: 58168, 58169
 - peel or tail: 58167
The analysis will be displayed for the unrolled and/or vectorized loops: 58170

Section 20.1.1: Binary (unrolled and/or vectorized) loop #58170
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:543-549,555-559.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
8% of peak computational performance is used (2.60 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.00 to 7.00 cycles (1.43x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is poorly vectorized.
Only 17% of vector register length is used (average across all SSE/AVX instructions).
By fully vectorizing your loop, you can lower the cost of an iteration from 10.00 to 1.62 cycles (6.15x speedup).
43% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 16% of SSE/AVX loads are used in vector version.
 - 0% of SSE/AVX stores are used in vector version.
 - 88% of SSE/AVX multiply instructions are used in vector version.
 - 40% of SSE/AVX instructions that are not load, store, addition, subtraction nor multiply instructions are used in vector version.
Since your execution units are vector units, only a fully vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 4 FMA (fused multiply-add) operations.
Presence of both ADD/SUB and MUL operations.
Workaround(s):
Try to change order in which elements are evaluated (using parentheses) in arithmetic expressions containing both ADD/SUB and MUL operations to enable your compiler to generate FMA instructions wherever possible.
Estimated speedup by perfect pairing: 1.02x.
For instance a + b*c is a valid FMA (MUL then ADD).
However (a+b)* c cannot be translated into an FMA (ADD then MUL).


All innermost loops were analyzed.

[MAQAO] Info: Assuming lines 397 and 398 correspond to the same source loop
[MAQAO] Info: Assuming lines 405 and 404 correspond to the same source loop
Section 21: Function: nnp_scale_acsf
====================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 21.1: Source loop ending at line 357
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58237 (355-357)
 - 58238 (355-357)
 - 58239 (355-357)
 - 58240 (355-357)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58239
 - unrolled and/or vectorized peel or tail: 58238
 - peel or tail: 58237, 58240
The analysis will be displayed for the unrolled and/or vectorized loops: 58239

Section 21.1.1: Binary (unrolled and/or vectorized) loop #58239
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:355-357.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 3.60 cycles (1.11x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 21.2: Source loop ending at line 362
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58233 (360-362)
 - 58234 (360-362)
 - 58235 (360-362)
 - 58236 (360-362)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58235
 - unrolled and/or vectorized peel or tail: 58234
 - peel or tail: 58233, 58236
The analysis will be displayed for the unrolled and/or vectorized loops: 58235

Section 21.2.1: Binary (unrolled and/or vectorized) loop #58235
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:360-362.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 3.60 cycles (1.11x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 21.3: Source loop ending at line 369
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58230 (366-369)
 - 58231 (366-369)
 - 58232 (366-369)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58231
 - peel or tail: 58230, 58232
The analysis will be displayed for the unrolled and/or vectorized loops: 58231

Section 21.3.1: Binary (unrolled and/or vectorized) loop #58231
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:366-369.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
6% of peak computational performance is used (2.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 6.00 cycles (5.33x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 6.00 cycles (5.33x speedup).

FMA
---
Detected 16 FMA (fused multiply-add) operations.

Section 21.4: Source loop ending at line 376
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58227 (373-376)
 - 58228 (373-376)
 - 58229 (373-376)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58228
 - peel or tail: 58227, 58229
The analysis will be displayed for the unrolled and/or vectorized loops: 58228

Section 21.4.1: Binary (unrolled and/or vectorized) loop #58228
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:373-376.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
6% of peak computational performance is used (2.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 6.00 cycles (5.33x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 6.00 cycles (5.33x speedup).

FMA
---
Detected 16 FMA (fused multiply-add) operations.

Section 21.5: Source loop ending at line 384
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58224 (381-384)
 - 58225 (381-384)
 - 58226 (381-384)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58225
 - peel or tail: 58224, 58226
The analysis will be displayed for the unrolled and/or vectorized loops: 58225

Section 21.5.1: Binary (unrolled and/or vectorized) loop #58225
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:381-384.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
7% of peak computational performance is used (2.50 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 8.00 cycles (4.00x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 8.00 cycles (4.00x speedup).

FMA
---
Detected 16 FMA (fused multiply-add) operations.

Section 21.6: Source loop ending at line 391
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58221 (388-391)
 - 58222 (388-391)
 - 58223 (388-391)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58222
 - peel or tail: 58221, 58223
The analysis will be displayed for the unrolled and/or vectorized loops: 58222

Section 21.6.1: Binary (unrolled and/or vectorized) loop #58222
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:388-391.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
7% of peak computational performance is used (2.50 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 8.00 cycles (4.00x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 8.00 cycles (4.00x speedup).

FMA
---
Detected 16 FMA (fused multiply-add) operations.

Section 21.7: Source loop ending at line 397
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58218 (395-398)
 - 58219 (395-397)
 - 58220 (395-398)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58219
 - peel or tail: 58218, 58220
The analysis will be displayed for the unrolled and/or vectorized loops: 58219

Section 21.7.1: Binary (unrolled and/or vectorized) loop #58219
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:395-397.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
6% of peak computational performance is used (2.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 6.00 cycles (5.33x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 6.00 cycles (5.33x speedup).

FMA
---
Detected 16 FMA (fused multiply-add) operations.

Section 21.8: Source loop ending at line 405
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58215 (402-405)
 - 58216 (402-404)
 - 58217 (402-405)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58216
 - peel or tail: 58215, 58217
The analysis will be displayed for the unrolled and/or vectorized loops: 58216

Section 21.8.1: Binary (unrolled and/or vectorized) loop #58216
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:402-404.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
6% of peak computational performance is used (2.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 6.00 cycles (5.33x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 6.00 cycles (5.33x speedup).

FMA
---
Detected 16 FMA (fused multiply-add) operations.

Section 21.9: Source loop ending at line 410
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58213 (409-410)
 - 58214 (409-410)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58214
 - peel or tail: 58213
The analysis will be displayed for the unrolled and/or vectorized loops: 58214

Section 21.9.1: Binary (unrolled and/or vectorized) loop #58214
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:409-410.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 21.10: Source loop ending at line 414
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58211 (413-414)
 - 58212 (413-414)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58212
 - peel or tail: 58211
The analysis will be displayed for the unrolled and/or vectorized loops: 58212

Section 21.10.1: Binary (unrolled and/or vectorized) loop #58212
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:413-414.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 21.11: Source loop ending at line 422
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58208 (422-422)
 - 58209 (422-422)
 - 58210 (422-422)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58210
 - unrolled and/or vectorized peel or tail: 58209
 - peel or tail: 58208
The analysis will be displayed for the unrolled and/or vectorized loops: 58210

Section 21.11.1: Binary (unrolled and/or vectorized) loop #58210
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:422.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is partially vectorized.
Only 31% of vector register length is used (average across all SSE/AVX instructions).

63% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 0% of SSE/AVX loads are used in vector version.


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 22.80 cycles (1.40x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 22.80 cycles (1.40x speedup).

Section 21.12: Source loop ending at line 431
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58204 (431-431)
 - 58205 (431-431)
 - 58206 (431-431)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58206
 - unrolled and/or vectorized peel or tail: 58205
 - peel or tail: 58204
The analysis will be displayed for the unrolled and/or vectorized loops: 58206

Section 21.12.1: Binary (unrolled and/or vectorized) loop #58206
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:431.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is partially vectorized.
Only 31% of vector register length is used (average across all SSE/AVX instructions).

63% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 0% of SSE/AVX loads are used in vector version.


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 22.80 cycles (1.40x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 22.80 cycles (1.40x speedup).

Section 21.13: Source loop ending at line 440
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58198 (440-440)
 - 58199 (440-440)
 - 58200 (440-440)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58200
 - unrolled and/or vectorized peel or tail: 58199
 - peel or tail: 58198
The analysis will be displayed for the unrolled and/or vectorized loops: 58200

Section 21.13.1: Binary (unrolled and/or vectorized) loop #58200
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:440.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is partially vectorized.
Only 31% of vector register length is used (average across all SSE/AVX instructions).

63% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 0% of SSE/AVX loads are used in vector version.


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 23.00 cycles (1.39x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 23.00 cycles (1.39x speedup).

Section 21.14: Source loop ending at line 448
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58194 (448-448)
 - 58195 (448-448)
 - 58196 (448-448)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58196
 - unrolled and/or vectorized peel or tail: 58195
 - peel or tail: 58194
The analysis will be displayed for the unrolled and/or vectorized loops: 58196

Section 21.14.1: Binary (unrolled and/or vectorized) loop #58196
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:448.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is partially vectorized.
Only 31% of vector register length is used (average across all SSE/AVX instructions).

63% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 0% of SSE/AVX loads are used in vector version.


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 23.20 cycles (1.38x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 23.20 cycles (1.38x speedup).

Section 21.15: Source loop ending at line 459
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58188 (459-459)
 - 58189 (459-459)
 - 58190 (459-459)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58190
 - unrolled and/or vectorized peel or tail: 58189
 - peel or tail: 58188
The analysis will be displayed for the unrolled and/or vectorized loops: 58190

Section 21.15.1: Binary (unrolled and/or vectorized) loop #58190
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:459.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is poorly vectorized.
Only 20% of vector register length is used (average across all SSE/AVX instructions).

33% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 0% of SSE/AVX loads are used in vector version.
 - 0% of SSE/AVX stores are used in vector version.


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 16.00 cycles (2.00x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 16.00 cycles (2.00x speedup).

Section 21.16: Source loop ending at line 466
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58183 (466-466)
 - 58184 (466-466)
 - 58185 (466-466)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58185
 - unrolled and/or vectorized peel or tail: 58184
 - peel or tail: 58183
The analysis will be displayed for the unrolled and/or vectorized loops: 58185

Section 21.16.1: Binary (unrolled and/or vectorized) loop #58185
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:466.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is poorly vectorized.
Only 20% of vector register length is used (average across all SSE/AVX instructions).

33% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 0% of SSE/AVX loads are used in vector version.
 - 0% of SSE/AVX stores are used in vector version.


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 16.00 cycles (2.00x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 16.00 cycles (2.00x speedup).

Section 21.17: Source loop ending at line 473
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58178 (473-473)
 - 58179 (473-473)
 - 58180 (473-473)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58180
 - unrolled and/or vectorized peel or tail: 58179
 - peel or tail: 58178
The analysis will be displayed for the unrolled and/or vectorized loops: 58180

Section 21.17.1: Binary (unrolled and/or vectorized) loop #58180
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:473.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is poorly vectorized.
Only 20% of vector register length is used (average across all SSE/AVX instructions).

33% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 0% of SSE/AVX loads are used in vector version.
 - 0% of SSE/AVX stores are used in vector version.


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 16.00 cycles (2.00x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 16.00 cycles (2.00x speedup).

Section 21.18: Source loop ending at line 479
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58173 (479-479)
 - 58174 (479-479)
 - 58175 (479-479)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58175
 - unrolled and/or vectorized peel or tail: 58174
 - peel or tail: 58173
The analysis will be displayed for the unrolled and/or vectorized loops: 58175

Section 21.18.1: Binary (unrolled and/or vectorized) loop #58175
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:479.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is poorly vectorized.
Only 20% of vector register length is used (average across all SSE/AVX instructions).

33% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 0% of SSE/AVX loads are used in vector version.
 - 0% of SSE/AVX stores are used in vector version.


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 16.00 cycles (2.00x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 16.00 cycles (2.00x speedup).


All innermost loops were analyzed.

[MAQAO] Info: Assuming lines 1266 and 1267 correspond to the same source loop
[MAQAO] Info: Assuming lines 1266 and 1268 correspond to the same source loop
Section 22: Function: nnp_neighbor_create
=========================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 22.1: Source loop ending at line 1266
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58241 (1268-1268)
 - 58242 (1268-1268)
 - 58243 (1267-1267)
 - 58244 (1267-1267)
 - 58245 (1266-1266)
 - 58246 (1266-1266)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58242, 58244, 58246
 - peel or tail: 58241, 58243, 58245
The analysis will be displayed for the unrolled and/or vectorized loops: 58242, 58244, 58246

Section 22.1.1: Binary (unrolled and/or vectorized) loop #58242
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1268.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 22.1.2: Binary (unrolled and/or vectorized) loop #58244
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1267.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 22.1.3: Binary (unrolled and/or vectorized) loop #58246
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1266.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


All innermost loops were analyzed.

Section 23: Function: nnp_compute_pbc_copies
============================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 23.1: Source loop ending at line 1401
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58249
and is not unrolled or unrolled with no peel/tail loop.

Section 23.1.1: Binary loop #58249
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1400-1401.

The related source loop is not unrolled or unrolled with no peel/tail loop.
1% of peak computational performance is used (0.57 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 1.75 to 1.50 cycles (1.17x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 12% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 1.75 to 0.19 cycles (9.33x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 1.75 to 1.60 cycles (1.09x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions

Section 23.2: Source loop ending at line 1404
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58248
and is not unrolled or unrolled with no peel/tail loop.

Section 23.2.1: Binary loop #58248
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1403-1404.

The related source loop is not unrolled or unrolled with no peel/tail loop.
1% of peak computational performance is used (0.57 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 1.75 to 1.50 cycles (1.17x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 12% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 1.75 to 0.19 cycles (9.33x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 1.75 to 1.60 cycles (1.09x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions

Section 23.3: Source loop ending at line 1407
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58247
and is not unrolled or unrolled with no peel/tail loop.

Section 23.3.1: Binary loop #58247
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1406-1407.

The related source loop is not unrolled or unrolled with no peel/tail loop.
1% of peak computational performance is used (0.57 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 1.75 to 1.50 cycles (1.17x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 12% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 1.75 to 0.19 cycles (9.33x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 1.75 to 1.60 cycles (1.09x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions


All innermost loops were analyzed.

Section 24: Function: nnp_compute_neighbors
===========================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 24.1: Source loop ending at line 1337
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58255
and is not unrolled or unrolled with no peel/tail loop.

Section 24.1.1: Binary loop #58255
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1331-1337.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 3 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 24.1.1.1: Path #1
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.60 to 0.19 cycles (13.87x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 24.1.1.2: Path #2
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 5.80 to 2.20 cycles (2.64x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 5.80 to 0.62 cycles (9.28x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 24.1.1.3: Path #3
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 3.20 to 2.00 cycles (1.60x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 3.20 to 0.25 cycles (12.80x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 24.1.1.4: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 3.87 to 0.35 cycles (10.92x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 24.2: Source loop ending at line 1353
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58254
and is not unrolled or unrolled with no peel/tail loop.

Section 24.2.1: Binary loop #58254
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1341-1353.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 5 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 24.2.1.1: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 8.80 to 0.74 cycles (11.93x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 24.2.1.2: Path #1
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 3.00 to 1.50 cycles (2.00x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 3.00 to 0.37 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 3.00 to 2.25 cycles (1.33x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 24.2.1.3: Path #2
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 13.50 to 4.00 cycles (3.37x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 13.50 to 1.25 cycles (10.80x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 13.50 to 11.60 cycles (1.16x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 24.2.1.4: Path #3
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.50 to 2.50 cycles (4.20x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 10.50 to 0.75 cycles (14.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 10.50 to 8.60 cycles (1.22x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 24.2.1.5: Path #4
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.00 to 2.50 cycles (4.00x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 10.00 to 0.72 cycles (13.91x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 10.00 to 8.40 cycles (1.19x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 24.2.1.6: Path #5
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 7.00 to 2.50 cycles (2.80x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 7.00 to 0.59 cycles (11.79x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 7.00 to 5.40 cycles (1.30x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop



All innermost loops were analyzed.

[MAQAO] Info: No innermost loops in the function nnp_acsf_mp_nnp_neighbor_release_
[MAQAO] Info: Assuming lines 774 and 776 correspond to the same source loop
Section 25: Function: nnp_sort_ele
==================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 25.1: Source loop ending at line 765
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58261
and is not unrolled or unrolled with no peel/tail loop.

Section 25.1.1: Binary loop #58261
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:764-765.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.60 to 0.44 cycles (10.51x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 25.2: Source loop ending at line 774
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58257 (768-774)
 - 58258 (771-776)
 - 58259 (770-776)
 - 58260 (768-774)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58259
 - unrolled and/or vectorized peel or tail: 58257
 - peel or tail: 58258, 58260
The analysis will be displayed for the unrolled and/or vectorized loops: 58259

Section 25.2.1: Binary (unrolled and/or vectorized) loop #58259
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:770-776.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Masked instructions
-------------------
Detected masked instructions.
Vector registers are partially exploited, which is expected if your loop is irregular or mixes elements of different sizes.
Workaround(s):
If your loop is irregular, try to remove or hoist conditional structures out of your loop. If it mixes elements of different sizes, try to uniformize them.


All innermost loops were analyzed.

Section 26: Function: nnp_sort_acsf
===================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 26.1: Source loop ending at line 809
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58282
and is not unrolled or unrolled with no peel/tail loop.

Section 26.1.1: Binary loop #58282
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:806-809.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 4 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 26.1.1.1: Path #1
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 4.75 to 2.50 cycles (1.90x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.75 to 0.59 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.1.1.2: Path #2
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.80 to 2.50 cycles (1.12x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.80 to 0.35 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.1.1.3: Path #3
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.75 to 2.50 cycles (1.10x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.75 to 0.34 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.1.1.4: Path #4
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.50 to 0.31 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.1.1.5: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 3.20 to 0.40 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 26.2: Source loop ending at line 824
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58280
and is not unrolled or unrolled with no peel/tail loop.

Section 26.2.1: Binary loop #58280
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:819-824.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=16
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 16 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.40 to 6.50 cycles (1.29x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.40 to 1.05 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.3: Source loop ending at line 839
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58279
and is not unrolled or unrolled with no peel/tail loop.

Section 26.3.1: Binary loop #58279
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:833-839.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. If you really need to analyze all of the 36 paths individually, rerun with max-paths=36
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 36 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 12.40 to 10.50 cycles (1.18x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 12.40 to 1.55 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.4: Source loop ending at line 855
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58278
and is not unrolled or unrolled with no peel/tail loop.

Section 26.4.1: Binary loop #58278
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:848-855.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 8 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 26.4.1.1: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.81 to 0.60 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 26.4.1.2: Path #1
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.50 to 0.19 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.4.1.3: Path #2
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.00 to 0.25 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.4.1.4: Path #3
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 3.60 to 0.45 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.4.1.5: Path #4
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.4.1.6: Path #5
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 5.60 to 0.70 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.4.1.7: Path #6
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 6.00 to 0.75 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.4.1.8: Path #7
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.40 to 5.50 cycles (1.53x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.40 to 1.05 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.4.1.9: Path #8
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 7.40 to 5.50 cycles (1.35x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 7.40 to 0.92 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.5: Source loop ending at line 867
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58274
and is not unrolled or unrolled with no peel/tail loop.

Section 26.5.1: Binary loop #58274
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:864-867.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 4 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 26.5.1.1: Path #1
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 4.75 to 2.50 cycles (1.90x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.75 to 0.59 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.5.1.2: Path #2
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.80 to 2.50 cycles (1.12x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.80 to 0.35 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.5.1.3: Path #3
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 2.75 to 2.50 cycles (1.10x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.75 to 0.34 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.5.1.4: Path #4
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.50 to 0.31 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.5.1.5: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 3.20 to 0.40 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 26.6: Source loop ending at line 882
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58272
and is not unrolled or unrolled with no peel/tail loop.

Section 26.6.1: Binary loop #58272
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:877-882.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=16
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 16 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.40 to 6.50 cycles (1.29x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.40 to 1.05 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.7: Source loop ending at line 897
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58271
and is not unrolled or unrolled with no peel/tail loop.

Section 26.7.1: Binary loop #58271
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:891-897.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. If you really need to analyze all of the 36 paths individually, rerun with max-paths=36
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 36 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 12.40 to 10.50 cycles (1.18x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 12.40 to 1.55 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.8: Source loop ending at line 913
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58270
and is not unrolled or unrolled with no peel/tail loop.

Section 26.8.1: Binary loop #58270
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:906-913.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 8 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 26.8.1.1: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.84 to 0.60 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 26.8.1.2: Path #1
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.50 to 0.19 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.8.1.3: Path #2
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.00 to 0.25 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.8.1.4: Path #3
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 3.60 to 0.45 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.8.1.5: Path #4
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.8.1.6: Path #5
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 5.60 to 0.70 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.8.1.7: Path #6
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 6.00 to 0.75 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.8.1.8: Path #7
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 8.40 to 7.50 cycles (1.12x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 8.40 to 1.05 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.8.1.9: Path #8
=========================

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 7.60 to 0.95 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.9: Source loop ending at line 930
============================================

Composition and unrolling
-------------------------
It is composed of the loop 58269
and is not unrolled or unrolled with no peel/tail loop.

Section 26.9.1: Binary loop #58269
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:922-930.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=10
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 10 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.60 to 7.00 cycles (1.51x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 10.60 to 1.32 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 26.10: Source loop ending at line 946
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58268
and is not unrolled or unrolled with no peel/tail loop.

Section 26.10.1: Binary loop #58268
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:937-946.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=11
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 11 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 12.40 to 7.50 cycles (1.65x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 12.40 to 0.97 cycles (12.80x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


All innermost loops were analyzed.

[MAQAO] Info: No innermost loops in the function nnp_acsf_mp_nnp_swapang_
[MAQAO] Info: No innermost loops in the function nnp_acsf_mp_nnp_swaprad_
Section 27: Function: nnp_write_acsf
====================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 27.1: Source loop ending at line 1211
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58286 (1211-1211)
 - 58287 (1211-1211)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58287
 - peel or tail: 58286
The analysis will be displayed for the unrolled and/or vectorized loops: 58287

Section 27.1.1: Binary (unrolled and/or vectorized) loop #58287
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1211.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 27.2: Source loop ending at line 1219
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58285
and is not unrolled or unrolled with no peel/tail loop.

Section 27.2.1: Binary loop #58285
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1216-1219.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 20.80 to 1.84 cycles (11.28x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 27.3: Source loop ending at line 1226
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58284
and is not unrolled or unrolled with no peel/tail loop.

Section 27.3.1: Binary loop #58284
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1221-1226.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 25.20 to 2.41 cycles (10.47x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


All innermost loops were analyzed.

[MAQAO] Info: Assuming lines 1098 and 1099 correspond to the same source loop
Section 28: Function: nnp_init_acsf_groups
==========================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 28.1: Source loop ending at line 1072
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58310 (1068-1072)
 - 58311 (1068-1072)
and is multi-versionned.
The analysis will be displayed for all loops

Section 28.1.1: Binary loop #58310
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1068-1072.

The related source loop is multi-versionned.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=9
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 9 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.20 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.20 to 3.50 cycles (2.91x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 10.20 to 0.37 cycles (27.77x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 28.1.2: Binary loop #58311
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1068-1072.

The related source loop is multi-versionned.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=9
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 9 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.20 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.20 to 3.50 cycles (2.91x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 10.20 to 0.37 cycles (27.77x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 28.2: Source loop ending at line 1088
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58309
and is not unrolled or unrolled with no peel/tail loop.

Section 28.2.1: Binary loop #58309
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1081-1088.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=10
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 10 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.13 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 7.80 to 3.50 cycles (2.23x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 7.80 to 0.28 cycles (27.86x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 28.3: Source loop ending at line 1098
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58303 (1099-1099)
 - 58304 (1098-1098)
and is multi-versionned.
The analysis will be displayed for all loops

Section 28.3.1: Binary loop #58303
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1099.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 7.00 to 5.00 cycles (1.40x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is partially vectorized.
Only 45% of vector register length is used (average across all SSE/AVX instructions).

87% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 87% of SSE/AVX loads are used in vector version.
 - 87% of SSE/AVX stores are used in vector version.

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 7.00 to 6.20 cycles (1.13x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 28.3.2: Binary loop #58304
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1098.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 7.00 to 4.50 cycles (1.56x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is partially vectorized.
Only 43% of vector register length is used (average across all SSE/AVX instructions).

82% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 77% of SSE/AVX loads are used in vector version.
 - 87% of SSE/AVX stores are used in vector version.

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 7.00 to 6.40 cycles (1.09x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 28.4: Source loop ending at line 1101
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58302
and is not unrolled or unrolled with no peel/tail loop.

Section 28.4.1: Binary loop #58302
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1100-1101.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 28.5: Source loop ending at line 1104
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58301
and is not unrolled or unrolled with no peel/tail loop.

Section 28.5.1: Binary loop #58301
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1103-1104.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 28.6: Source loop ending at line 1127
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58300
and is not unrolled or unrolled with no peel/tail loop.

Section 28.6.1: Binary loop #58300
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1115-1127.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 3 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 28.6.1.1: Path #1
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 5% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.20 to 0.09 cycles (44.80x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 28.6.1.2: Path #2
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 7.50 to 2.00 cycles (3.75x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 7.50 to 0.21 cycles (35.29x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 7.50 to 6.80 cycles (1.10x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 28.6.1.3: Path #3
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 59.50 to 7.40 cycles (8.04x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 12% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 59.50 to 3.73 cycles (15.93x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 59.50 to 51.60 cycles (1.15x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions

Section 28.6.1.4: Average path
==============================

Vectorization
-------------
Your loop is probably not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 23.73 to 1.35 cycles (17.62x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 28.7: Source loop ending at line 1150
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58298
and is not unrolled or unrolled with no peel/tail loop.

Section 28.7.1: Binary loop #58298
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1133-1150.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=10
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 10 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.01 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 72.75 to 10.00 cycles (7.27x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 72.75 to 4.56 cycles (15.95x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 72.75 to 61.80 cycles (1.18x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions

Section 28.8: Source loop ending at line 1153
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58290
and is not unrolled or unrolled with no peel/tail loop.

Section 28.8.1: Binary loop #58290
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1112,1153.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.25 to 0.16 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 28.9: Source loop ending at line 1164
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 58296
and is not unrolled or unrolled with no peel/tail loop.

Section 28.9.1: Binary loop #58296
==================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1160-1164.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=9
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 9 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.17 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 11.80 to 3.50 cycles (3.37x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 12% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 11.80 to 0.84 cycles (14.00x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 28.10: Source loop ending at line 1182
==============================================

Composition and unrolling
-------------------------
It is composed of the loop 58294
and is not unrolled or unrolled with no peel/tail loop.

Section 28.10.1: Binary loop #58294
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:1172-1182.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
 - Ignoring paths for analysis
 - Too many paths. Rerun with max-paths=10
 - RecMII not computed since number of paths is unknown or > max_paths

Try to simplify control and/or increase the maximum number of paths per function/loop through the 'max-paths-nb' option.

This loop has 10 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.11 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 9.00 to 3.50 cycles (2.57x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 9.00 to 0.30 cycles (30.28x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


All innermost loops were analyzed.

[MAQAO] Info: Assuming lines 319 and 318 correspond to the same source loop
[MAQAO] Info: Assuming lines 328 and 327 correspond to the same source loop
Section 29: Function: nnp_check_extrapolation
=============================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/nnp_acsf.F

Section 29.1: Source loop ending at line 319
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58315 (313-319)
 - 58316 (311-318)
 - 58317 (313-319)
and is unrolled by 13 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58316
 - peel or tail: 58315, 58317
The analysis will be displayed for the unrolled and/or vectorized loops: 58316

Section 29.1.1: Binary (unrolled and/or vectorized) loop #58316
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:311-318.

It is main loop of related source loop which is unrolled by 13 (including vectorization).
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

5% of peak computational performance is used (1.60 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 29.1.1.1: Path #1
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.00 to 4.00 cycles (2.50x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 10.00 to 6.60 cycles (1.52x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Masked instructions
-------------------
Detected masked instructions.
Vector registers are partially exploited, which is expected if your loop is irregular or mixes elements of different sizes.
Workaround(s):
If your loop is irregular, try to remove or hoist conditional structures out of your loop. If it mixes elements of different sizes, try to uniformize them.

Section 29.1.1.2: Path #2
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 7.00 to 2.20 cycles (3.18x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 7.00 to 3.80 cycles (1.84x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 29.1.1.3: Average path
==============================

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Masked instructions
-------------------
Detected masked instructions.
Vector registers are partially exploited, which is expected if your loop is irregular or mixes elements of different sizes.
Workaround(s):
If your loop is irregular, try to remove or hoist conditional structures out of your loop. If it mixes elements of different sizes, try to uniformize them.

Section 29.2: Source loop ending at line 328
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 58312 (322-328)
 - 58313 (311-327)
 - 58314 (322-328)
and is unrolled by 13 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 58313
 - peel or tail: 58312, 58314
The analysis will be displayed for the unrolled and/or vectorized loops: 58313

Section 29.2.1: Binary (unrolled and/or vectorized) loop #58313
===============================================================

The loop is defined in /home/crm98/cp2k_master/build/src/nnp_acsf.F:311,322-327.

It is main loop of related source loop which is unrolled by 13 (including vectorization).
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

5% of peak computational performance is used (1.60 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 29.2.1.1: Path #1
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.00 to 4.00 cycles (2.50x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 10.00 to 6.60 cycles (1.52x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Masked instructions
-------------------
Detected masked instructions.
Vector registers are partially exploited, which is expected if your loop is irregular or mixes elements of different sizes.
Workaround(s):
If your loop is irregular, try to remove or hoist conditional structures out of your loop. If it mixes elements of different sizes, try to uniformize them.

Section 29.2.1.2: Path #2
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 7.00 to 2.20 cycles (3.18x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 7.00 to 3.80 cycles (1.84x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 29.2.1.3: Average path
==============================

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Masked instructions
-------------------
Detected masked instructions.
Vector registers are partially exploited, which is expected if your loop is irregular or mixes elements of different sizes.
Workaround(s):
If your loop is irregular, try to remove or hoist conditional structures out of your loop. If it mixes elements of different sizes, try to uniformize them.


All innermost loops were analyzed.

[MAQAO] Info: Assuming lines 797 and 798 correspond to the same source loop
Section 30: Function: helium_intpot_nnp
=======================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/motion/helium_interactions.F

Section 30.1: Source loop ending at line 697
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 148610 (697-697)
 - 148611 (697-697)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 148611
 - peel or tail: 148610
The analysis will be displayed for the unrolled and/or vectorized loops: 148611

Section 30.1.1: Binary (unrolled and/or vectorized) loop #148611
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:697.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 30.2: Source loop ending at line 718
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 148606 (713-718)
 - 148607 (713-718)
and is multi-versionned.
The analysis will be displayed for all loops

Section 30.2.1: Binary loop #148606
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:713-718.

The related source loop is multi-versionned.
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 30.2.1.1: Path #1
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 5.50 to 0.44 cycles (12.57x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 5.50 to 5.00 cycles (1.10x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 30.2.1.2: Path #2
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 5% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.00 to 0.09 cycles (21.33x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 30.2.1.3: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 3.75 to 0.27 cycles (14.12x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)



Section 30.2.2: Binary loop #148607
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:713-718.

The related source loop is multi-versionned.
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 30.2.2.1: Path #1
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 5.00 to 0.40 cycles (12.61x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 30.2.2.2: Path #2
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 5% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.00 to 0.09 cycles (21.33x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 30.2.2.3: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 3.50 to 0.25 cycles (14.27x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 30.3: Source loop ending at line 730
============================================

Composition and unrolling
-------------------------
It is composed of the loop 148599
and is not unrolled or unrolled with no peel/tail loop.

Section 30.3.1: Binary loop #148599
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:705,716,725-730.

The related source loop is not unrolled or unrolled with no peel/tail loop.
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
2% of peak computational performance is used (0.67 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 30.3.1.1: Path #1
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 12.00 to 6.20 cycles (1.94x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is probably not vectorized.
Only 12% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 12.00 to 1.09 cycles (10.97x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 2 FMA (fused multiply-add) operations.
Presence of both ADD/SUB and MUL operations.
Workaround(s):
Try to change order in which elements are evaluated (using parentheses) in arithmetic expressions containing both ADD/SUB and MUL operations to enable your compiler to generate FMA instructions wherever possible.
For instance a + b*c is a valid FMA (MUL then ADD).
However (a+b)* c cannot be translated into an FMA (ADD then MUL).

Section 30.3.1.2: Path #2
=========================

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.06 cycles (16.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 30.3.1.3: Average path
==============================

Vectorization
-------------
Your loop is probably not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 6.50 to 0.58 cycles (11.24x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


FMA
---
Detected 1 FMA (fused multiply-add) operations.
Presence of both ADD/SUB and MUL operations.
Workaround(s):
Try to change order in which elements are evaluated (using parentheses) in arithmetic expressions containing both ADD/SUB and MUL operations to enable your compiler to generate FMA instructions wherever possible.
For instance a + b*c is a valid FMA (MUL then ADD).
However (a+b)* c cannot be translated into an FMA (ADD then MUL).

Section 30.4: Source loop ending at line 733
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 148600 (733-733)
 - 148601 (733-733)
 - 148602 (733-733)
 - 148603 (733-733)
 - 148604 (733-733)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 148603
 - unrolled and/or vectorized peel or tail: 148600, 148602
 - peel or tail: 148601, 148604
The analysis will be displayed for the unrolled and/or vectorized loops: 148603

Section 30.4.1: Binary (unrolled and/or vectorized) loop #148603
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:733.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 30.5: Source loop ending at line 752
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 148597 (752-752)
 - 148598 (752-752)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 148598
 - peel or tail: 148597
The analysis will be displayed for the unrolled and/or vectorized loops: 148598

Section 30.5.1: Binary (unrolled and/or vectorized) loop #148598
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:752.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 30.6: Source loop ending at line 755
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 148595 (755-755)
 - 148596 (755-755)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 148596
 - peel or tail: 148595
The analysis will be displayed for the unrolled and/or vectorized loops: 148596

Section 30.6.1: Binary (unrolled and/or vectorized) loop #148596
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:755.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 30.7: Source loop ending at line 758
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 148593 (758-758)
 - 148594 (758-758)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 148594
 - peel or tail: 148593
The analysis will be displayed for the unrolled and/or vectorized loops: 148594

Section 30.7.1: Binary (unrolled and/or vectorized) loop #148594
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:758.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 30.8: Source loop ending at line 773
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 148590 (773-773)
 - 148591 (773-773)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 148591
 - peel or tail: 148590
The analysis will be displayed for the unrolled and/or vectorized loops: 148591

Section 30.8.1: Binary (unrolled and/or vectorized) loop #148591
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:773.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 128 out of 512 bits (SSE/AVX-128 instructions on AVX-512 processors).
By fully vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.50 cycles (2.00x speedup).
All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).
Since your execution units are vector units, only a fully vectorized loop can use their full power.

Workaround(s):
 - Pass to your compiler a micro-architecture specialization option:
  * use axHost or xHost
 - Use vector aligned instructions:
  1) align your arrays on 64 bytes boundaries: compile with -align array64byte (remark: not affects arrays in COMMON blocks).
  2) inform your compiler that your arrays are vector aligned: Append !DIR$ VECTOR ALIGNED to the loop if all accessed arrays are aligned, or !DIR$ ASSUME_ALIGNED FOO: 64 if only FOO is aligned.
 - Use the LOOP COUNT directive


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


Section 30.9: Source loop ending at line 780
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 148587 (777-780)
 - 148588 (777-780)
 - 148589 (777-780)
and is unrolled by 8 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 148589
 - unrolled and/or vectorized peel or tail: 148588
 - peel or tail: 148587
The analysis will be displayed for the unrolled and/or vectorized loops: 148589

Section 30.9.1: Binary (unrolled and/or vectorized) loop #148589
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:777-780.

It is main loop of related source loop which is unrolled by 8 (including vectorization).
4% of peak computational performance is used (1.51 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 31.80 to 24.50 cycles (1.30x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 14% of vector register length is used (average across all SSE/AVX instructions).
By fully vectorizing your loop, you can lower the cost of an iteration from 31.80 to 4.33 cycles (7.35x speedup).
14% of SSE/AVX instructions are used in vector version (process two or more data elements in vector registers):
 - 0% of SSE/AVX loads are used in vector version.
 - 0% of SSE/AVX stores are used in vector version.
Since your execution units are vector units, only a fully vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 24 FMA (fused multiply-add) operations.

Section 30.10: Source loop ending at line 797
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 148571 (798-798)
 - 148572 (798-798)
 - 148573 (798-798)
 - 148574 (798-798)
 - 148575 (797-797)
 - 148576 (797-797)
 - 148578 (797-797)
 - 148579 (797-797)
 - 148580 (797-797)
 - 148581 (797-797)
 - 148582 (797-797)
 - 148583 (797-797)
and is multi-versionned.
The analysis will be displayed for all loops

Section 30.10.1: Binary loop #148571
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:798.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 30.10.2: Binary loop #148572
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:798.

The related source loop is multi-versionned.
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 30.10.3: Binary loop #148573
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:798.

The related source loop is multi-versionned.
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 30.10.4: Binary loop #148574
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:798.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 30.10.5: Binary loop #148575
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:797.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 30.10.6: Binary loop #148576
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:797.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 30.10.7: Binary loop #148578
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:797.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 30.10.8: Binary loop #148579
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:797.

The related source loop is multi-versionned.
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 30.10.9: Binary loop #148580
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:797.

The related source loop is multi-versionned.
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 30.10.10: Binary loop #148581
=====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:797.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 30.10.11: Binary loop #148582
=====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:797.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.06 cycles (16.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 30.10.12: Binary loop #148583
=====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:797.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 30.11: Source loop ending at line 805
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 148570
and is not unrolled or unrolled with no peel/tail loop.

Section 30.11.1: Binary loop #148570
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:805.

The related source loop is not unrolled or unrolled with no peel/tail loop.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 5.75 to 0.72 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 30.12: Source loop ending at line 808
=============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 148550 (808-808)
 - 148551 (808-808)
 - 148552 (808-808)
 - 148554 (808-808)
 - 148555 (808-808)
 - 148556 (808-808)
 - 148560 (808-808)
 - 148561 (808-808)
 - 148562 (808-808)
 - 148563 (808-808)
 - 148565 (808-808)
 - 148566 (808-808)
 - 148567 (808-808)
and is multi-versionned.
The analysis will be displayed for all loops

Section 30.12.1: Binary loop #148550
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 30.12.2: Binary loop #148551
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 30.12.3: Binary loop #148552
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 13% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.53 cycles (7.56x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 30.12.4: Binary loop #148554
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
1% of peak computational performance is used (0.50 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 8.00 to 1.40 cycles (5.71x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 8.00 to 1.40 cycles (5.71x speedup).

Masked instructions
-------------------
Detected masked instructions.
Vector registers are partially exploited, which is expected if your loop is irregular or mixes elements of different sizes.
Workaround(s):
If your loop is irregular, try to remove or hoist conditional structures out of your loop. If it mixes elements of different sizes, try to uniformize them.


Section 30.12.5: Binary loop #148555
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
1% of peak computational performance is used (0.50 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 32.00 to 4.00 cycles (8.00x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 32.00 to 3.20 cycles (10.00x speedup).


Section 30.12.6: Binary loop #148556
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
1% of peak computational performance is used (0.50 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by execution of divide and square root operations (the divide/square root unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 8.00 to 1.40 cycles (5.71x speedup).

Workaround(s):
 - Reduce the number of division or square root instructions:
  * If denominator is constant over iterations, use reciprocal (replace x/y with x*(1/y)). Check precision impact. This will be done by your compiler with no-prec-div or Ofast
 - Check whether you really need double precision. If not, switch to single precision to speedup execution


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Expensive FP math instructions/calls
------------------------------------
Detected performance impact from expensive FP math instructions/calls.
By removing/reexpressing them, you can lower the cost of an iteration from 8.00 to 1.40 cycles (5.71x speedup).

Masked instructions
-------------------
Detected masked instructions.
Vector registers are partially exploited, which is expected if your loop is irregular or mixes elements of different sizes.
Workaround(s):
If your loop is irregular, try to remove or hoist conditional structures out of your loop. If it mixes elements of different sizes, try to uniformize them.


Section 30.12.7: Binary loop #148560
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
2% of peak computational performance is used (0.83 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.20 to 0.15 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 30.12.8: Binary loop #148561
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
10% of peak computational performance is used (3.33 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 30.12.9: Binary loop #148562
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by reading data from caches/RAM (load units are a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 3.80 cycles (1.05x speedup).

Workaround(s):
 - Read less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 30.12.10: Binary loop #148563
=====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
2% of peak computational performance is used (0.83 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.20 to 0.15 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 30.12.11: Binary loop #148565
=====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.06 cycles (16.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 30.12.12: Binary loop #148566
=====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 30.12.13: Binary loop #148567
=====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:808.

The related source loop is multi-versionned.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is probably not vectorized.
Only 13% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 2.80 to 0.27 cycles (10.35x speedup).
Store and arithmetical SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 30.13: Source loop ending at line 816
=============================================

Composition and unrolling
-------------------------
It is composed of the loop 148548
and is not unrolled or unrolled with no peel/tail loop.

Section 30.13.1: Binary loop #148548
====================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_interactions.F:811-816.

The related source loop is not unrolled or unrolled with no peel/tail loop.
This loop has 3 execution paths.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 30.13.1.1: Path #1
==========================

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.00 to 0.12 cycles (16.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 30.13.1.2: Path #2
==========================

Vectorization
-------------
Your loop is not vectorized.
Only 12% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 7.40 to 0.62 cycles (11.92x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 30.13.1.3: Path #3
==========================

Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 11.50 to 0.75 cycles (15.33x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 30.13.1.4: Average path
===============================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 6.97 to 0.50 cycles (13.97x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)



All innermost loops were analyzed.

[MAQAO] Info: No innermost loops in the function helium_nnp_mp_helium_nnp_print_
Section 31: Function: helium_nnp_print_energies
===============================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/motion/helium_nnp.F

Section 31.1: Source loop ending at line 231
============================================

Composition and unrolling
-------------------------
It is composed of the loop 149234
and is not unrolled or unrolled with no peel/tail loop.

Section 31.1.1: Binary loop #149234
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:230-231.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 4.40 to 0.56 cycles (7.82x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 31.2: Source loop ending at line 237
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 149224 (237-237)
 - 149225 (237-237)
 - 149226 (237-237)
 - 149228 (237-237)
 - 149229 (237-237)
 - 149230 (237-237)
 - 149231 (237-237)
 - 149232 (237-237)
 - 149233 (237-237)
and is multi-versionned.
The analysis will be displayed for all loops

Section 31.2.1: Binary loop #149224
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:237.

The related source loop is multi-versionned.
9% of peak computational performance is used (3.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Masked instructions
-------------------
Detected masked instructions.
Vector registers are partially exploited, which is expected if your loop is irregular or mixes elements of different sizes.
Workaround(s):
If your loop is irregular, try to remove or hoist conditional structures out of your loop. If it mixes elements of different sizes, try to uniformize them.

FMA
---
Presence of both ADD/SUB and MUL operations.
Workaround(s):
 - Pass to your compiler a micro-architecture specialization option:
  * use axHost or xHost
 - Try to change order in which elements are evaluated (using parentheses) in arithmetic expressions containing both ADD/SUB and MUL operations to enable your compiler to generate FMA instructions wherever possible.
For instance a + b*c is a valid FMA (MUL then ADD).
However (a+b)* c cannot be translated into an FMA (ADD then MUL).



Section 31.2.2: Binary loop #149225
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:237.

The related source loop is multi-versionned.
37% of peak computational performance is used (12.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Performance is limited by execution of FP multiply or FMA (fused multiply-add) operations (the FP multiply/FMA unit is a bottleneck).

By removing all these bottlenecks, you can lower the cost of an iteration from 4.00 to 2.80 cycles (1.43x speedup).

Workaround(s):
Reduce the number of FP multiply/FMA instructions

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

FMA
---
Detected 16 FMA (fused multiply-add) operations.


Section 31.2.3: Binary loop #149226
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:237.

The related source loop is multi-versionned.
9% of peak computational performance is used (3.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Masked instructions
-------------------
Detected masked instructions.
Vector registers are partially exploited, which is expected if your loop is irregular or mixes elements of different sizes.
Workaround(s):
If your loop is irregular, try to remove or hoist conditional structures out of your loop. If it mixes elements of different sizes, try to uniformize them.

FMA
---
Presence of both ADD/SUB and MUL operations.
Workaround(s):
 - Pass to your compiler a micro-architecture specialization option:
  * use axHost or xHost
 - Try to change order in which elements are evaluated (using parentheses) in arithmetic expressions containing both ADD/SUB and MUL operations to enable your compiler to generate FMA instructions wherever possible.
For instance a + b*c is a valid FMA (MUL then ADD).
However (a+b)* c cannot be translated into an FMA (ADD then MUL).



Section 31.2.4: Binary loop #149228
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:237.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 31.2.5: Binary loop #149229
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:237.

The related source loop is multi-versionned.
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 31.2.6: Binary loop #149230
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:237.

The related source loop is multi-versionned.
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 31.2.7: Binary loop #149231
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:237.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 31.2.8: Binary loop #149232
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:237.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 31.2.9: Binary loop #149233
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:237.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 31.3: Source loop ending at line 240
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 149215 (240-240)
 - 149216 (240-240)
 - 149218 (240-240)
 - 149219 (240-240)
 - 149220 (240-240)
 - 149221 (240-240)
 - 149222 (240-240)
 - 149223 (240-240)
and is multi-versionned.
The analysis will be displayed for all loops

Section 31.3.1: Binary loop #149215
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:240.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.12 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 31.3.2: Binary loop #149216
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:240.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 31.3.3: Binary loop #149218
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:240.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 31.3.4: Binary loop #149219
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:240.

The related source loop is multi-versionned.
3% of peak computational performance is used (1.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 31.3.5: Binary loop #149220
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:240.

The related source loop is multi-versionned.
12% of peak computational performance is used (4.00 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


Section 31.3.6: Binary loop #149221
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:240.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 31.3.7: Binary loop #149222
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:240.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 1.00 to 0.06 cycles (16.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


Section 31.3.8: Binary loop #149223
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:240.

The related source loop is multi-versionned.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


All innermost loops were analyzed.

Section 32: Function: helium_nnp_print_force_sigma
==================================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/motion/helium_nnp.F

Section 32.1: Source loop ending at line 270
============================================

Composition and unrolling
-------------------------
It is composed of the loop 149236
and is not unrolled or unrolled with no peel/tail loop.

Section 32.1.1: Binary loop #149236
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:269-270.

The related source loop is not unrolled or unrolled with no peel/tail loop.
7% of peak computational performance is used (2.25 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is not vectorized.
8 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 4.00 to 0.50 cycles (8.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 3 FMA (fused multiply-add) operations.


All innermost loops were analyzed.

Section 33: Function: helium_nnp_print_expol
============================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/motion/helium_nnp.F

Section 33.1: Source loop ending at line 315
============================================

Composition and unrolling
-------------------------
It is composed of the loop 149241
and is not unrolled or unrolled with no peel/tail loop.

Section 33.1.1: Binary loop #149241
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:309-315.

The related source loop is not unrolled or unrolled with no peel/tail loop.
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
2% of peak computational performance is used (0.70 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 33.1.1.1: Path #1
=========================

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 10.00 to 4.00 cycles (2.50x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 11% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 10.00 to 0.84 cycles (11.85x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

FMA
---
Detected 3 FMA (fused multiply-add) operations.

Section 33.1.1.2: Path #2
=========================

Vectorization
-------------
Your loop is not vectorized.
16 data elements could be processed at once in vector registers.
By vectorizing your loop, you can lower the cost of an iteration from 2.00 to 0.12 cycles (16.00x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 33.1.1.3: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
Only 8% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 6.00 to 0.48 cycles (12.39x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


FMA
---
Detected 1.50 FMA (fused multiply-add) operations.

Section 33.2: Source loop ending at line 319
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 149239 (319-319)
 - 149240 (319-319)
and is unrolled by 4 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 149240
 - peel or tail: 149239
The analysis will be displayed for the unrolled and/or vectorized loops: 149240

Section 33.2.1: Binary (unrolled and/or vectorized) loop #149240
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:319.

It is main loop of related source loop which is unrolled by 4 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 33.3: Source loop ending at line 325
============================================

Composition and unrolling
-------------------------
It is composed of the loop 149238
and is not unrolled or unrolled with no peel/tail loop.

Section 33.3.1: Binary loop #149238
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:324-325.

The related source loop is not unrolled or unrolled with no peel/tail loop.
Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.31 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Code clean check
----------------
Detected a slowdown caused by scalar integer instructions (typically used for address computation).
By removing them, you can lower the cost of an iteration from 9.60 to 3.50 cycles (2.74x speedup).
Workaround(s):
 - Try to reorganize arrays of structures to structures of arrays
 - Consider to permute loops (see vectorization gain report)
 - To reference allocatable arrays, use "allocatable" instead of "pointer" pointers or qualify them with the "contiguous" attribute (Fortran 2008)
 - For structures, limit to one indirection. For example, use a_b%c instead of a%b%c with a_b set to a%b before this loop


Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 9.60 to 1.12 cycles (8.53x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.


All innermost loops were analyzed.

[MAQAO] Info: Assuming lines 134 and 133 correspond to the same source loop
Section 34: Function: helium_init_nnp
=====================================

Recompile with -sox to prevent CQA from suggesting already used flags
Code for this function has been specialized for Zen 4, zen 5, Skylake_SP (processor). For execution on another machine, recompile on it or with explicit target (example for a Haswell machine: use -xCORE-AVX2, see compiler manual for full list).
These loops are supposed to be defined in: /home/crm98/cp2k_master/build/src/motion/helium_nnp.F

Section 34.1: Source loop ending at line 134
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 149250 (134-134)
 - 149251 (134-134)
 - 149252 (134-134)
 - 149253 (134-134)
 - 149254 (134-134)
 - 149256 (122-133)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 149253
 - unrolled and/or vectorized peel or tail: 149250, 149252
 - peel or tail: 149251, 149254, 149256
The analysis will be displayed for the unrolled and/or vectorized loops: 149253

Section 34.1.1: Binary (unrolled and/or vectorized) loop #149253
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:134.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).


Execution units bottlenecks
---------------------------
Performance is limited by writing data to caches/RAM (the store unit is a bottleneck).

Workaround(s):
 - Write less array elements
 - Provide more information to your compiler:
  * hardcode the bounds of the corresponding 'for' loop


512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high

Section 34.2: Source loop ending at line 147
============================================

Composition and unrolling
-------------------------
It is composed of the loop 149249
and is not unrolled or unrolled with no peel/tail loop.

Section 34.2.1: Binary loop #149249
===================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:89,142-147.

The related source loop is not unrolled or unrolled with no peel/tail loop.
The structure of this loop is probably <if then [else] end>.

The presence of multiple execution paths is typically the main/first bottleneck.
Try to simplify control inside loop: ideally, try to remove all conditional expressions, for example by (if applicable):
 - hoisting them (moving them outside the loop)
 - turning them into conditional moves, MIN or MAX


Ex: if (x<0) x=0 => x = max(0,x) (Fortran instrinsic procedure)

Warnings:
Detected a function call instruction: ignoring called function instructions.
Rerun with --follow-calls=append to include them to analysis  or with --follow-calls=inline to simulate inlining.
0% of peak computational performance is used (0.00 out of 64.00 FLOP per cycle (GFLOPS @ 1GHz))

Section 34.2.1.1: Path #1
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 12.40 to 1.44 cycles (8.63x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 34.2.1.2: Path #2
=========================

Vectorization
-------------
Your loop is not vectorized.
Only 9% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 6.75 to 0.49 cycles (13.64x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

Section 34.2.1.3: Average path
==============================

Vectorization
-------------
Your loop is not vectorized.
Only 10% of vector register length is used (average across all SSE/AVX instructions).
By vectorizing your loop, you can lower the cost of an iteration from 9.57 to 0.97 cycles (9.91x speedup).
All SSE/AVX instructions are used in scalar version (process only one data element in vector registers).
Since your execution units are vector units, only a vectorized loop can use their full power.

Workaround(s):
 - Try another compiler or update/tune your current one:
  * use the vec-report option to understand why your loop was not vectorized. If "existence of vector dependences", try the IVDEP directive. If, using IVDEP, "vectorization possible but seems inefficient", try the VECTOR ALWAYS directive.
 - Remove inter-iterations dependences from your loop and make it unit-stride:
  * If your arrays have 2 or more dimensions, check whether elements are accessed contiguously and, otherwise, try to permute loops accordingly:
Fortran storage order is column-major: do i do j a(i,j) = b(i,j) (slow, non stride 1) => do i do j a(j,i) = b(i,j) (fast, stride 1)
  * If your loop streams arrays of structures (AoS), try to use structures of arrays instead (SoA):
do i a(i)%x = b(i)%x (slow, non stride 1) => do i a%x(i) = b%x(i) (fast, stride 1)


Section 34.3: Source loop ending at line 153
============================================

Composition and unrolling
-------------------------
It is composed of the following loops [ID (first-last source line)]:
 - 149242 (153-153)
 - 149243 (153-153)
 - 149244 (153-153)
 - 149245 (153-153)
 - 149246 (153-153)
 - 149247 (153-153)
and is unrolled by 16 (including vectorization).

The following loops are considered as:
 - unrolled and/or vectorized main: 149246
 - unrolled and/or vectorized peel or tail: 149243, 149245
 - peel or tail: 149242, 149244, 149247
The analysis will be displayed for the unrolled and/or vectorized loops: 149246

Section 34.3.1: Binary (unrolled and/or vectorized) loop #149246
================================================================

The loop is defined in /home/crm98/cp2k_master/build/src/motion/helium_nnp.F:153.

It is main loop of related source loop which is unrolled by 16 (including vectorization).
17% of peak computational performance is used (5.71 out of 32.00 FLOP per cycle (GFLOPS @ 1GHz))

Vectorization
-------------
Your loop is vectorized, but using only 256 out of 512 bits (AVX/AVX2 instructions on AVX-512 processors).

All SSE/AVX instructions are used in vector version (process two or more data elements in vector registers).

Workaround(s):
Read the "512-bits vectorization" report at "Potential" confidence level.

Execution units bottlenecks
---------------------------
Found no such bottlenecks but see expert reports for more complex bottlenecks.

512-bits vectorization
----------------------
On some x86 processors supporting 512-bits vectorization, compilers are often too conservative and limit vectorization to 256 bits. Performance can then be improved by enforcing 512-bits vectorization, especially with many vectorized and high trip count loops. 512-bits vectorization performance overhead (compared to 256-bits) is generally lower on newer processors.

Workaround(s):
Recompile with -qopt-zmm-usage=high


All innermost loops were analyzed.

[MAQAO] Info: Rerun CQA with conf=hint,expert to display more advanced reports or conf=all to display them with default reports.
